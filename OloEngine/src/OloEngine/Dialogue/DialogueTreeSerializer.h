#pragma once

// DialogueTreeSerializer is declared in AssetSerializer.h alongside all other asset serializers.
// This header provides a convenient include for code that specifically needs the dialogue serializer.
#include "OloEngine/Asset/AssetSerializer.h"
