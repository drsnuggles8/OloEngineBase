// Auto-generated by OloHeaderTool — DO NOT EDIT MANUALLY
// Re-generate with: cmake --build build --target GenerateBindings

///////////////////////////////////////////////////////////////////////////////////////////
// AudioSoundGraphComponent                                                       //
///////////////////////////////////////////////////////////////////////////////////////////

static float AudioSoundGraphComponent_GetVolume(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    return comp.VolumeMultiplier;
}

static void AudioSoundGraphComponent_SetVolume(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    comp.VolumeMultiplier = value;
}

static float AudioSoundGraphComponent_GetPitch(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    return comp.PitchMultiplier;
}

static void AudioSoundGraphComponent_SetPitch(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    comp.PitchMultiplier = value;
}

static bool AudioSoundGraphComponent_GetLooping(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    return comp.Looping;
}

static void AudioSoundGraphComponent_SetLooping(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    comp.Looping = value;
}

static bool AudioSoundGraphComponent_GetPlayOnAwake(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    return comp.PlayOnAwake;
}

static void AudioSoundGraphComponent_SetPlayOnAwake(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    comp.PlayOnAwake = value;
}

static u64 AudioSoundGraphComponent_GetSoundGraphHandle(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    return comp.SoundGraphHandle;
}

static void AudioSoundGraphComponent_SetSoundGraphHandle(UUID entityID, u64 value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSoundGraphComponent>();
    comp.SoundGraphHandle = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// AudioSourceComponent                                                           //
///////////////////////////////////////////////////////////////////////////////////////////

static float AudioSourceComponent_GetVolume(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.VolumeMultiplier;
}

static void AudioSourceComponent_SetVolume(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.VolumeMultiplier = value;
    if (comp.Source)
        comp.Source->SetVolume(value);
}

static float AudioSourceComponent_GetPitch(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.PitchMultiplier;
}

static void AudioSourceComponent_SetPitch(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.PitchMultiplier = value;
    if (comp.Source)
        comp.Source->SetPitch(value);
}

static bool AudioSourceComponent_GetPlayOnAwake(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.PlayOnAwake;
}

static void AudioSourceComponent_SetPlayOnAwake(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.PlayOnAwake = value;
}

static bool AudioSourceComponent_GetLooping(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.Looping;
}

static void AudioSourceComponent_SetLooping(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.Looping = value;
    if (comp.Source)
        comp.Source->SetLooping(value);
}

static bool AudioSourceComponent_GetSpatialization(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.Spatialization;
}

static void AudioSourceComponent_SetSpatialization(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.Spatialization = value;
    if (comp.Source)
        comp.Source->SetSpatialization(value);
}

static int AudioSourceComponent_GetAttenuationModel(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return static_cast<int>(comp.Config.AttenuationModel);
}

static void AudioSourceComponent_SetAttenuationModel(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.AttenuationModel = static_cast<AttenuationModelType>(value);
    if (comp.Source)
        comp.Source->SetAttenuationModel(comp.Config.AttenuationModel);
}

static float AudioSourceComponent_GetRollOff(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.RollOff;
}

static void AudioSourceComponent_SetRollOff(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.RollOff = value;
    if (comp.Source)
        comp.Source->SetRollOff(value);
}

static float AudioSourceComponent_GetMinGain(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.MinGain;
}

static void AudioSourceComponent_SetMinGain(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.MinGain = value;
    if (comp.Source)
        comp.Source->SetMinGain(value);
}

static float AudioSourceComponent_GetMaxGain(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.MaxGain;
}

static void AudioSourceComponent_SetMaxGain(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.MaxGain = value;
    if (comp.Source)
        comp.Source->SetMaxGain(value);
}

static float AudioSourceComponent_GetMinDistance(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.MinDistance;
}

static void AudioSourceComponent_SetMinDistance(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.MinDistance = value;
    if (comp.Source)
        comp.Source->SetMinDistance(value);
}

static float AudioSourceComponent_GetMaxDistance(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.MaxDistance;
}

static void AudioSourceComponent_SetMaxDistance(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.MaxDistance = value;
    if (comp.Source)
        comp.Source->SetMaxDistance(value);
}

static float AudioSourceComponent_GetConeInnerAngle(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.ConeInnerAngle;
}

static void AudioSourceComponent_SetConeInnerAngle(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.ConeInnerAngle = value;
    if (comp.Source)
        comp.Source->SetCone(comp.Config.ConeInnerAngle, comp.Config.ConeOuterAngle, comp.Config.ConeOuterGain);
}

static float AudioSourceComponent_GetConeOuterAngle(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.ConeOuterAngle;
}

static void AudioSourceComponent_SetConeOuterAngle(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.ConeOuterAngle = value;
    if (comp.Source)
        comp.Source->SetCone(comp.Config.ConeInnerAngle, comp.Config.ConeOuterAngle, comp.Config.ConeOuterGain);
}

static float AudioSourceComponent_GetConeOuterGain(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.ConeOuterGain;
}

static void AudioSourceComponent_SetConeOuterGain(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.ConeOuterGain = value;
    if (comp.Source)
        comp.Source->SetCone(comp.Config.ConeInnerAngle, comp.Config.ConeOuterAngle, comp.Config.ConeOuterGain);
}

static float AudioSourceComponent_GetDopplerFactor(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    return comp.Config.DopplerFactor;
}

static void AudioSourceComponent_SetDopplerFactor(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<AudioSourceComponent>();
    comp.Config.DopplerFactor = value;
    if (comp.Source)
        comp.Source->SetDopplerFactor(value);
}

///////////////////////////////////////////////////////////////////////////////////////////
// BoxCollider2DComponent                                                         //
///////////////////////////////////////////////////////////////////////////////////////////

static void BoxCollider2DComponent_GetOffset(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    *outValue = comp.Offset;
}

static void BoxCollider2DComponent_SetOffset(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Offset = *value;
}

static void BoxCollider2DComponent_GetSize(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    *outValue = comp.Size;
}

static void BoxCollider2DComponent_SetSize(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Size = *value;
}

static float BoxCollider2DComponent_GetDensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    return comp.Density;
}

static void BoxCollider2DComponent_SetDensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    comp.Density = value;
}

static float BoxCollider2DComponent_GetFriction(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    return comp.Friction;
}

static void BoxCollider2DComponent_SetFriction(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    comp.Friction = value;
}

static float BoxCollider2DComponent_GetRestitution(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    return comp.Restitution;
}

static void BoxCollider2DComponent_SetRestitution(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<BoxCollider2DComponent>();
    comp.Restitution = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// BoxCollider3DComponent                                                         //
///////////////////////////////////////////////////////////////////////////////////////////

static void BoxCollider3DComponent_GetHalfExtents(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider3DComponent>();
    *outValue = comp.m_HalfExtents;
}

static void BoxCollider3DComponent_SetHalfExtents(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_HalfExtents = *value;
}

static void BoxCollider3DComponent_GetOffset(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider3DComponent>();
    *outValue = comp.m_Offset;
}

static void BoxCollider3DComponent_SetOffset(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<BoxCollider3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Offset = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// CameraComponent                                                                //
///////////////////////////////////////////////////////////////////////////////////////////

static int CameraComponent_GetProjectionType(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return static_cast<int>(comp.Camera.GetProjectionType());
}

static void CameraComponent_SetProjectionType(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetProjectionType(static_cast<SceneCamera::ProjectionType>(value));
}

static float CameraComponent_GetPerspectiveFOV(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Camera.GetPerspectiveVerticalFOV();
}

static void CameraComponent_SetPerspectiveFOV(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetPerspectiveVerticalFOV(value);
}

static float CameraComponent_GetPerspectiveNearClip(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Camera.GetPerspectiveNearClip();
}

static void CameraComponent_SetPerspectiveNearClip(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetPerspectiveNearClip(value);
}

static float CameraComponent_GetPerspectiveFarClip(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Camera.GetPerspectiveFarClip();
}

static void CameraComponent_SetPerspectiveFarClip(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetPerspectiveFarClip(value);
}

static float CameraComponent_GetOrthographicSize(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Camera.GetOrthographicSize();
}

static void CameraComponent_SetOrthographicSize(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetOrthographicSize(value);
}

static float CameraComponent_GetOrthographicNearClip(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Camera.GetOrthographicNearClip();
}

static void CameraComponent_SetOrthographicNearClip(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetOrthographicNearClip(value);
}

static float CameraComponent_GetOrthographicFarClip(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Camera.GetOrthographicFarClip();
}

static void CameraComponent_SetOrthographicFarClip(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Camera.SetOrthographicFarClip(value);
}

static bool CameraComponent_GetPrimary(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.Primary;
}

static void CameraComponent_SetPrimary(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.Primary = value;
}

static bool CameraComponent_GetFixedAspectRatio(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    return comp.FixedAspectRatio;
}

static void CameraComponent_SetFixedAspectRatio(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CameraComponent>();
    comp.FixedAspectRatio = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// CapsuleCollider3DComponent                                                     //
///////////////////////////////////////////////////////////////////////////////////////////

static float CapsuleCollider3DComponent_GetRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CapsuleCollider3DComponent>();
    return comp.m_Radius;
}

static void CapsuleCollider3DComponent_SetRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CapsuleCollider3DComponent>();
    comp.m_Radius = value;
}

static float CapsuleCollider3DComponent_GetHalfHeight(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CapsuleCollider3DComponent>();
    return comp.m_HalfHeight;
}

static void CapsuleCollider3DComponent_SetHalfHeight(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CapsuleCollider3DComponent>();
    comp.m_HalfHeight = value;
}

static void CapsuleCollider3DComponent_GetOffset(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CapsuleCollider3DComponent>();
    *outValue = comp.m_Offset;
}

static void CapsuleCollider3DComponent_SetOffset(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CapsuleCollider3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Offset = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// CharacterController3DComponent                                                 //
///////////////////////////////////////////////////////////////////////////////////////////

static float CharacterController3DComponent_GetSlopeLimit(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    return comp.m_SlopeLimitDeg;
}

static void CharacterController3DComponent_SetSlopeLimit(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    comp.m_SlopeLimitDeg = value;
}

static float CharacterController3DComponent_GetStepOffset(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    return comp.m_StepOffset;
}

static void CharacterController3DComponent_SetStepOffset(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    comp.m_StepOffset = value;
}

static float CharacterController3DComponent_GetJumpPower(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    return comp.m_JumpPower;
}

static void CharacterController3DComponent_SetJumpPower(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    comp.m_JumpPower = value;
}

static bool CharacterController3DComponent_GetDisableGravity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    return comp.m_DisableGravity;
}

static void CharacterController3DComponent_SetDisableGravity(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CharacterController3DComponent>();
    comp.m_DisableGravity = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// CinematicComponent                                                             //
///////////////////////////////////////////////////////////////////////////////////////////

static u64 CinematicComponent_GetSequence(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    return comp.Sequence;
}

static void CinematicComponent_SetSequence(UUID entityID, u64 value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    comp.Sequence = value;
}

static bool CinematicComponent_GetPlayOnStart(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    return comp.PlayOnStart;
}

static void CinematicComponent_SetPlayOnStart(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    comp.PlayOnStart = value;
}

static bool CinematicComponent_GetLoop(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    return comp.Loop;
}

static void CinematicComponent_SetLoop(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    comp.Loop = value;
}

static float CinematicComponent_GetPlaybackSpeed(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    return comp.PlaybackSpeed;
}

static void CinematicComponent_SetPlaybackSpeed(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CinematicComponent>();
    comp.PlaybackSpeed = value;
}

static bool CinematicComponent_GetPlaying(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    return comp.Playing;
}

static void CinematicComponent_SetPlaying(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CinematicComponent>();
    comp.Playing = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// CircleCollider2DComponent                                                      //
///////////////////////////////////////////////////////////////////////////////////////////

static void CircleCollider2DComponent_GetOffset(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    *outValue = comp.Offset;
}

static void CircleCollider2DComponent_SetOffset(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Offset = *value;
}

static float CircleCollider2DComponent_GetRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    return comp.Radius;
}

static void CircleCollider2DComponent_SetRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    comp.Radius = value;
}

static float CircleCollider2DComponent_GetDensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    return comp.Density;
}

static void CircleCollider2DComponent_SetDensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    comp.Density = value;
}

static float CircleCollider2DComponent_GetFriction(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    return comp.Friction;
}

static void CircleCollider2DComponent_SetFriction(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    comp.Friction = value;
}

static float CircleCollider2DComponent_GetRestitution(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    return comp.Restitution;
}

static void CircleCollider2DComponent_SetRestitution(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CircleCollider2DComponent>();
    comp.Restitution = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// CircleRendererComponent                                                        //
///////////////////////////////////////////////////////////////////////////////////////////

static void CircleRendererComponent_GetColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleRendererComponent>();
    *outValue = comp.Color;
}

static void CircleRendererComponent_SetColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleRendererComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Color = *value;
}

static float CircleRendererComponent_GetThickness(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleRendererComponent>();
    return comp.Thickness;
}

static void CircleRendererComponent_SetThickness(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CircleRendererComponent>();
    comp.Thickness = value;
}

static float CircleRendererComponent_GetFade(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<CircleRendererComponent>();
    return comp.Fade;
}

static void CircleRendererComponent_SetFade(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<CircleRendererComponent>();
    comp.Fade = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// DirectionalLightComponent                                                      //
///////////////////////////////////////////////////////////////////////////////////////////

static void DirectionalLightComponent_GetColor(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<DirectionalLightComponent>();
    *outValue = comp.m_Color;
}

static void DirectionalLightComponent_SetColor(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<DirectionalLightComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Color = *value;
}

static float DirectionalLightComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<DirectionalLightComponent>();
    return comp.m_Intensity;
}

static void DirectionalLightComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<DirectionalLightComponent>();
    comp.m_Intensity = value;
}

static bool DirectionalLightComponent_GetCastShadows(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<DirectionalLightComponent>();
    return comp.m_CastShadows;
}

static void DirectionalLightComponent_SetCastShadows(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<DirectionalLightComponent>();
    comp.m_CastShadows = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// LightProbeComponent                                                            //
///////////////////////////////////////////////////////////////////////////////////////////

static float LightProbeComponent_GetInfluenceRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeComponent>();
    return comp.m_InfluenceRadius;
}

static void LightProbeComponent_SetInfluenceRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<LightProbeComponent>();
    comp.m_InfluenceRadius = value;
}

static float LightProbeComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeComponent>();
    return comp.m_Intensity;
}

static void LightProbeComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<LightProbeComponent>();
    comp.m_Intensity = value;
}

static bool LightProbeComponent_GetActive(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeComponent>();
    return comp.m_Active;
}

static void LightProbeComponent_SetActive(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeComponent>();
    comp.m_Active = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// LightProbeVolumeComponent                                                      //
///////////////////////////////////////////////////////////////////////////////////////////

static void LightProbeVolumeComponent_GetBoundsMin(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    *outValue = comp.m_BoundsMin;
}

static void LightProbeVolumeComponent_SetBoundsMin(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_BoundsMin = *value;
    comp.m_Dirty = true;
}

static void LightProbeVolumeComponent_GetBoundsMax(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    *outValue = comp.m_BoundsMax;
}

static void LightProbeVolumeComponent_SetBoundsMax(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_BoundsMax = *value;
    comp.m_Dirty = true;
}

static float LightProbeVolumeComponent_GetSpacing(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    return comp.m_Spacing;
}

static void LightProbeVolumeComponent_SetSpacing(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    comp.m_Spacing = value;
    comp.m_Dirty = true;
}

static float LightProbeVolumeComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    return comp.m_Intensity;
}

static void LightProbeVolumeComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    comp.m_Intensity = value;
    comp.m_Dirty = true;
}

static bool LightProbeVolumeComponent_GetActive(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    return comp.m_Active;
}

static void LightProbeVolumeComponent_SetActive(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<LightProbeVolumeComponent>();
    comp.m_Active = value;
    comp.m_Dirty = true;
}

///////////////////////////////////////////////////////////////////////////////////////////
// MaterialComponent                                                              //
///////////////////////////////////////////////////////////////////////////////////////////

static void MaterialComponent_GetAlbedoColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<MaterialComponent>();
    *outValue = comp.m_Material.GetBaseColorFactor();
}

static void MaterialComponent_SetAlbedoColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<MaterialComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Material.SetBaseColorFactor(*value);
}

static float MaterialComponent_GetMetallic(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<MaterialComponent>();
    return comp.m_Material.GetMetallicFactor();
}

static void MaterialComponent_SetMetallic(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<MaterialComponent>();
    comp.m_Material.SetMetallicFactor(value);
}

static float MaterialComponent_GetRoughness(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<MaterialComponent>();
    return comp.m_Material.GetRoughnessFactor();
}

static void MaterialComponent_SetRoughness(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<MaterialComponent>();
    comp.m_Material.SetRoughnessFactor(value);
}

static void MaterialComponent_GetEmissive(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<MaterialComponent>();
    *outValue = comp.m_Material.GetEmissiveFactor();
}

static void MaterialComponent_SetEmissive(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<MaterialComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Material.SetEmissiveFactor(*value);
}

///////////////////////////////////////////////////////////////////////////////////////////
// NameplateComponent                                                             //
///////////////////////////////////////////////////////////////////////////////////////////

static bool NameplateComponent_GetEnabled(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    return comp.m_Enabled;
}

static void NameplateComponent_SetEnabled(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    comp.m_Enabled = value;
}

static bool NameplateComponent_GetShowHealthBar(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    return comp.m_ShowHealthBar;
}

static void NameplateComponent_SetShowHealthBar(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    comp.m_ShowHealthBar = value;
}

static bool NameplateComponent_GetShowManaBar(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    return comp.m_ShowManaBar;
}

static void NameplateComponent_SetShowManaBar(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    comp.m_ShowManaBar = value;
}

static void NameplateComponent_GetWorldOffset(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    *outValue = comp.m_WorldOffset;
}

static void NameplateComponent_SetWorldOffset(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_WorldOffset = *value;
}

static void NameplateComponent_GetBarSize(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    *outValue = comp.m_BarSize;
}

static void NameplateComponent_SetBarSize(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_BarSize = *value;
}

static void NameplateComponent_GetHealthBarColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    *outValue = comp.m_HealthBarColor;
}

static void NameplateComponent_SetHealthBarColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_HealthBarColor = *value;
}

static void NameplateComponent_GetManaBarColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    *outValue = comp.m_ManaBarColor;
}

static void NameplateComponent_SetManaBarColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_ManaBarColor = *value;
}

static void NameplateComponent_GetBarBackgroundColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    *outValue = comp.m_BarBackgroundColor;
}

static void NameplateComponent_SetBarBackgroundColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_BarBackgroundColor = *value;
}

static float NameplateComponent_GetManaBarGap(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NameplateComponent>();
    return comp.m_ManaBarGap;
}

static void NameplateComponent_SetManaBarGap(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<NameplateComponent>();
    comp.m_ManaBarGap = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// NavAgentComponent                                                              //
///////////////////////////////////////////////////////////////////////////////////////////

static float NavAgentComponent_GetMaxSpeed(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    return comp.m_MaxSpeed;
}

static void NavAgentComponent_SetMaxSpeed(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<NavAgentComponent>();
    comp.m_MaxSpeed = value;
}

static float NavAgentComponent_GetAcceleration(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    return comp.m_Acceleration;
}

static void NavAgentComponent_SetAcceleration(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<NavAgentComponent>();
    comp.m_Acceleration = value;
}

static float NavAgentComponent_GetStoppingDistance(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    return comp.m_StoppingDistance;
}

static void NavAgentComponent_SetStoppingDistance(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<NavAgentComponent>();
    comp.m_StoppingDistance = value;
}

static bool NavAgentComponent_GetLockYAxis(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    return comp.m_LockYAxis;
}

static void NavAgentComponent_SetLockYAxis(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    comp.m_LockYAxis = value;
}

static void NavAgentComponent_GetTargetPosition(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    *outValue = comp.m_TargetPosition;
}

static void NavAgentComponent_SetTargetPosition(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavAgentComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_TargetPosition = *value;
    comp.m_HasTarget = true;
    comp.m_HasPath = false;
}

///////////////////////////////////////////////////////////////////////////////////////////
// NavMeshBoundsComponent                                                         //
///////////////////////////////////////////////////////////////////////////////////////////

static void NavMeshBoundsComponent_GetMin(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavMeshBoundsComponent>();
    *outValue = comp.m_Min;
}

static void NavMeshBoundsComponent_SetMin(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavMeshBoundsComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Min = *value;
}

static void NavMeshBoundsComponent_GetMax(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavMeshBoundsComponent>();
    *outValue = comp.m_Max;
}

static void NavMeshBoundsComponent_SetMax(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<NavMeshBoundsComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Max = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// ParticleSystemComponent                                                        //
///////////////////////////////////////////////////////////////////////////////////////////

static bool ParticleSystemComponent_GetPlaying(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    return comp.System.Playing;
}

static void ParticleSystemComponent_SetPlaying(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    comp.System.Playing = value;
}

static bool ParticleSystemComponent_GetLooping(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    return comp.System.Looping;
}

static void ParticleSystemComponent_SetLooping(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    comp.System.Looping = value;
}

static float ParticleSystemComponent_GetEmissionRate(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    return comp.System.Emitter.RateOverTime;
}

static void ParticleSystemComponent_SetEmissionRate(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    comp.System.Emitter.RateOverTime = value;
}

static float ParticleSystemComponent_GetWindInfluence(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    return comp.System.WindInfluence;
}

static void ParticleSystemComponent_SetWindInfluence(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ParticleSystemComponent>();
    comp.System.WindInfluence = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// PhysicsJoint3DComponent                                                        //
///////////////////////////////////////////////////////////////////////////////////////////

static int PhysicsJoint3DComponent_GetJointType(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return static_cast<int>(comp.m_Type);
}

static void PhysicsJoint3DComponent_SetJointType(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_Type = static_cast<JointType3D>(value);
}

static void PhysicsJoint3DComponent_GetLocalAnchorA(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    *outValue = comp.m_LocalAnchorA;
}

static void PhysicsJoint3DComponent_SetLocalAnchorA(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_LocalAnchorA = *value;
}

static void PhysicsJoint3DComponent_GetLocalAnchorB(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    *outValue = comp.m_LocalAnchorB;
}

static void PhysicsJoint3DComponent_SetLocalAnchorB(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_LocalAnchorB = *value;
}

static void PhysicsJoint3DComponent_GetAxis(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    *outValue = comp.m_Axis;
}

static void PhysicsJoint3DComponent_SetAxis(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Axis = *value;
}

static float PhysicsJoint3DComponent_GetMinDistance(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_MinDistance;
}

static void PhysicsJoint3DComponent_SetMinDistance(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_MinDistance = value;
}

static float PhysicsJoint3DComponent_GetMaxDistance(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_MaxDistance;
}

static void PhysicsJoint3DComponent_SetMaxDistance(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_MaxDistance = value;
}

static float PhysicsJoint3DComponent_GetHingeMinAngleDeg(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_HingeMinAngleDeg;
}

static void PhysicsJoint3DComponent_SetHingeMinAngleDeg(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMinAngleDeg = value;
}

static float PhysicsJoint3DComponent_GetHingeMaxAngleDeg(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_HingeMaxAngleDeg;
}

static void PhysicsJoint3DComponent_SetHingeMaxAngleDeg(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMaxAngleDeg = value;
}

static float PhysicsJoint3DComponent_GetSliderMinLimit(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_SliderMinLimit;
}

static void PhysicsJoint3DComponent_SetSliderMinLimit(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMinLimit = value;
}

static float PhysicsJoint3DComponent_GetSliderMaxLimit(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_SliderMaxLimit;
}

static void PhysicsJoint3DComponent_SetSliderMaxLimit(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMaxLimit = value;
}

static float PhysicsJoint3DComponent_GetConeHalfAngleDeg(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_ConeHalfAngleDeg;
}

static void PhysicsJoint3DComponent_SetConeHalfAngleDeg(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_ConeHalfAngleDeg = value;
}

static float PhysicsJoint3DComponent_GetBreakForce(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_BreakForce;
}

static void PhysicsJoint3DComponent_SetBreakForce(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_BreakForce = value;
}

static float PhysicsJoint3DComponent_GetBreakTorque(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_BreakTorque;
}

static void PhysicsJoint3DComponent_SetBreakTorque(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_BreakTorque = value;
}

static int PhysicsJoint3DComponent_GetHingeMotorMode(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return static_cast<int>(comp.m_HingeMotorMode);
}

static void PhysicsJoint3DComponent_SetHingeMotorMode(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMotorMode = static_cast<JointMotorMode>(value);
}

static float PhysicsJoint3DComponent_GetHingeMotorTargetVelocityDeg(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_HingeMotorTargetVelocityDeg;
}

static void PhysicsJoint3DComponent_SetHingeMotorTargetVelocityDeg(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMotorTargetVelocityDeg = value;
}

static float PhysicsJoint3DComponent_GetHingeMotorTargetAngleDeg(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_HingeMotorTargetAngleDeg;
}

static void PhysicsJoint3DComponent_SetHingeMotorTargetAngleDeg(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMotorTargetAngleDeg = value;
}

static float PhysicsJoint3DComponent_GetHingeMaxMotorTorque(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_HingeMaxMotorTorque;
}

static void PhysicsJoint3DComponent_SetHingeMaxMotorTorque(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMaxMotorTorque = value;
}

static float PhysicsJoint3DComponent_GetHingeMaxFrictionTorque(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_HingeMaxFrictionTorque;
}

static void PhysicsJoint3DComponent_SetHingeMaxFrictionTorque(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_HingeMaxFrictionTorque = value;
}

static int PhysicsJoint3DComponent_GetSliderMotorMode(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return static_cast<int>(comp.m_SliderMotorMode);
}

static void PhysicsJoint3DComponent_SetSliderMotorMode(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMotorMode = static_cast<JointMotorMode>(value);
}

static float PhysicsJoint3DComponent_GetSliderMotorTargetVelocity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_SliderMotorTargetVelocity;
}

static void PhysicsJoint3DComponent_SetSliderMotorTargetVelocity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMotorTargetVelocity = value;
}

static float PhysicsJoint3DComponent_GetSliderMotorTargetPosition(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_SliderMotorTargetPosition;
}

static void PhysicsJoint3DComponent_SetSliderMotorTargetPosition(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMotorTargetPosition = value;
}

static float PhysicsJoint3DComponent_GetSliderMaxMotorForce(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_SliderMaxMotorForce;
}

static void PhysicsJoint3DComponent_SetSliderMaxMotorForce(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMaxMotorForce = value;
}

static float PhysicsJoint3DComponent_GetSliderMaxFrictionForce(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    return comp.m_SliderMaxFrictionForce;
}

static void PhysicsJoint3DComponent_SetSliderMaxFrictionForce(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PhysicsJoint3DComponent>();
    comp.m_SliderMaxFrictionForce = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// PointLightComponent                                                            //
///////////////////////////////////////////////////////////////////////////////////////////

static void PointLightComponent_GetColor(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PointLightComponent>();
    *outValue = comp.m_Color;
}

static void PointLightComponent_SetColor(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PointLightComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Color = *value;
}

static float PointLightComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PointLightComponent>();
    return comp.m_Intensity;
}

static void PointLightComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PointLightComponent>();
    comp.m_Intensity = value;
}

static float PointLightComponent_GetRange(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PointLightComponent>();
    return comp.m_Range;
}

static void PointLightComponent_SetRange(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<PointLightComponent>();
    comp.m_Range = value;
}

static bool PointLightComponent_GetCastShadows(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PointLightComponent>();
    return comp.m_CastShadows;
}

static void PointLightComponent_SetCastShadows(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<PointLightComponent>();
    comp.m_CastShadows = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// ProceduralSkyComponent                                                         //
///////////////////////////////////////////////////////////////////////////////////////////

static void ProceduralSkyComponent_GetSunDirection(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    *outValue = comp.m_SunDirection;
}

static void ProceduralSkyComponent_SetSunDirection(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_SunDirection = *value;
}

static float ProceduralSkyComponent_GetTurbidity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_Turbidity;
}

static void ProceduralSkyComponent_SetTurbidity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_Turbidity = value;
}

static float ProceduralSkyComponent_GetExposure(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_Exposure;
}

static void ProceduralSkyComponent_SetExposure(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_Exposure = value;
}

static float ProceduralSkyComponent_GetSunIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_SunIntensity;
}

static void ProceduralSkyComponent_SetSunIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_SunIntensity = value;
}

static float ProceduralSkyComponent_GetSunDiskSize(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_SunDiskSize;
}

static void ProceduralSkyComponent_SetSunDiskSize(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_SunDiskSize = value;
}

static bool ProceduralSkyComponent_GetShowSunDisk(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_ShowSunDisk;
}

static void ProceduralSkyComponent_SetShowSunDisk(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_ShowSunDisk = value;
}

static bool ProceduralSkyComponent_GetLinkSunToDirectionalLight(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_LinkSunToDirectionalLight;
}

static void ProceduralSkyComponent_SetLinkSunToDirectionalLight(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_LinkSunToDirectionalLight = value;
}

static bool ProceduralSkyComponent_GetEnableSkybox(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_EnableSkybox;
}

static void ProceduralSkyComponent_SetEnableSkybox(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_EnableSkybox = value;
}

static bool ProceduralSkyComponent_GetEnableIBL(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_EnableIBL;
}

static void ProceduralSkyComponent_SetEnableIBL(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_EnableIBL = value;
}

static float ProceduralSkyComponent_GetIBLIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    return comp.m_IBLIntensity;
}

static void ProceduralSkyComponent_SetIBLIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ProceduralSkyComponent>();
    comp.m_IBLIntensity = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// ReflectionProbeComponent                                                       //
///////////////////////////////////////////////////////////////////////////////////////////

static float ReflectionProbeComponent_GetInfluenceRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    return comp.m_InfluenceRadius;
}

static void ReflectionProbeComponent_SetInfluenceRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    comp.m_InfluenceRadius = value;
}

static float ReflectionProbeComponent_GetBlendDistance(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    return comp.m_BlendDistance;
}

static void ReflectionProbeComponent_SetBlendDistance(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    comp.m_BlendDistance = value;
}

static unsigned int ReflectionProbeComponent_GetResolution(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    return comp.m_Resolution;
}

static void ReflectionProbeComponent_SetResolution(UUID entityID, unsigned int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    comp.m_Resolution = value;
}

static float ReflectionProbeComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    return comp.m_Intensity;
}

static void ReflectionProbeComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    comp.m_Intensity = value;
}

static bool ReflectionProbeComponent_GetActive(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    return comp.m_Active;
}

static void ReflectionProbeComponent_SetActive(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<ReflectionProbeComponent>();
    comp.m_Active = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// Rigidbody2DComponent                                                           //
///////////////////////////////////////////////////////////////////////////////////////////

static int Rigidbody2DComponent_GetType(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    return static_cast<int>(comp.Type);
}

static void Rigidbody2DComponent_SetType(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    comp.Type = static_cast<Rigidbody2DComponent::BodyType>(value);
}

static bool Rigidbody2DComponent_GetFixedRotation(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    return comp.FixedRotation;
}

static void Rigidbody2DComponent_SetFixedRotation(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    comp.FixedRotation = value;
}

static void Rigidbody2DComponent_GetLinearVelocity(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    *outValue = comp.LinearVelocity;
}

static void Rigidbody2DComponent_SetLinearVelocity(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.LinearVelocity = *value;
}

static float Rigidbody2DComponent_GetAngularVelocity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    return comp.AngularVelocity;
}

static void Rigidbody2DComponent_SetAngularVelocity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<Rigidbody2DComponent>();
    comp.AngularVelocity = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// Rigidbody3DComponent                                                           //
///////////////////////////////////////////////////////////////////////////////////////////

static int Rigidbody3DComponent_GetBodyType(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    return static_cast<int>(comp.m_Type);
}

static void Rigidbody3DComponent_SetBodyType(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    comp.m_Type = static_cast<BodyType3D>(value);
}

static float Rigidbody3DComponent_GetMass(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    return comp.m_Mass;
}

static void Rigidbody3DComponent_SetMass(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    comp.m_Mass = value;
}

static float Rigidbody3DComponent_GetLinearDrag(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    return comp.m_LinearDrag;
}

static void Rigidbody3DComponent_SetLinearDrag(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    comp.m_LinearDrag = value;
}

static float Rigidbody3DComponent_GetAngularDrag(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    return comp.m_AngularDrag;
}

static void Rigidbody3DComponent_SetAngularDrag(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    comp.m_AngularDrag = value;
}

static bool Rigidbody3DComponent_GetDisableGravity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    return comp.m_DisableGravity;
}

static void Rigidbody3DComponent_SetDisableGravity(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    comp.m_DisableGravity = value;
}

static bool Rigidbody3DComponent_GetIsTrigger(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    return comp.m_IsTrigger;
}

static void Rigidbody3DComponent_SetIsTrigger(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<Rigidbody3DComponent>();
    comp.m_IsTrigger = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// SphereAreaLightComponent                                                       //
///////////////////////////////////////////////////////////////////////////////////////////

static void SphereAreaLightComponent_GetColor(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    *outValue = comp.m_Color;
}

static void SphereAreaLightComponent_SetColor(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Color = *value;
}

static float SphereAreaLightComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    return comp.m_Intensity;
}

static void SphereAreaLightComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    comp.m_Intensity = value;
}

static float SphereAreaLightComponent_GetRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    return comp.m_Radius;
}

static void SphereAreaLightComponent_SetRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    comp.m_Radius = value;
}

static float SphereAreaLightComponent_GetRange(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    return comp.m_Range;
}

static void SphereAreaLightComponent_SetRange(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    comp.m_Range = value;
}

static bool SphereAreaLightComponent_GetCastShadows(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    return comp.m_CastShadows;
}

static void SphereAreaLightComponent_SetCastShadows(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereAreaLightComponent>();
    comp.m_CastShadows = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// SphereCollider3DComponent                                                      //
///////////////////////////////////////////////////////////////////////////////////////////

static float SphereCollider3DComponent_GetRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereCollider3DComponent>();
    return comp.m_Radius;
}

static void SphereCollider3DComponent_SetRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SphereCollider3DComponent>();
    comp.m_Radius = value;
}

static void SphereCollider3DComponent_GetOffset(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereCollider3DComponent>();
    *outValue = comp.m_Offset;
}

static void SphereCollider3DComponent_SetOffset(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SphereCollider3DComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Offset = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// SpotLightComponent                                                             //
///////////////////////////////////////////////////////////////////////////////////////////

static void SpotLightComponent_GetColor(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    *outValue = comp.m_Color;
}

static void SpotLightComponent_SetColor(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Color = *value;
}

static float SpotLightComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    return comp.m_Intensity;
}

static void SpotLightComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SpotLightComponent>();
    comp.m_Intensity = value;
}

static float SpotLightComponent_GetRange(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    return comp.m_Range;
}

static void SpotLightComponent_SetRange(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SpotLightComponent>();
    comp.m_Range = value;
}

static float SpotLightComponent_GetInnerCutoff(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    return comp.m_InnerCutoff;
}

static void SpotLightComponent_SetInnerCutoff(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SpotLightComponent>();
    comp.m_InnerCutoff = value;
}

static float SpotLightComponent_GetOuterCutoff(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    return comp.m_OuterCutoff;
}

static void SpotLightComponent_SetOuterCutoff(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SpotLightComponent>();
    comp.m_OuterCutoff = value;
}

static bool SpotLightComponent_GetCastShadows(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    return comp.m_CastShadows;
}

static void SpotLightComponent_SetCastShadows(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpotLightComponent>();
    comp.m_CastShadows = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// SpriteRendererComponent                                                        //
///////////////////////////////////////////////////////////////////////////////////////////

static void SpriteRendererComponent_GetColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpriteRendererComponent>();
    *outValue = comp.Color;
}

static void SpriteRendererComponent_SetColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpriteRendererComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Color = *value;
}

static float SpriteRendererComponent_GetTilingFactor(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<SpriteRendererComponent>();
    return comp.TilingFactor;
}

static void SpriteRendererComponent_SetTilingFactor(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<SpriteRendererComponent>();
    comp.TilingFactor = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// StarNestSkyComponent                                                           //
///////////////////////////////////////////////////////////////////////////////////////////

static void StarNestSkyComponent_GetOffset(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    *outValue = comp.m_Offset;
}

static void StarNestSkyComponent_SetOffset(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Offset = *value;
}

static float StarNestSkyComponent_GetRotation1(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Rotation1;
}

static void StarNestSkyComponent_SetRotation1(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Rotation1 = value;
}

static float StarNestSkyComponent_GetRotation2(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Rotation2;
}

static void StarNestSkyComponent_SetRotation2(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Rotation2 = value;
}

static float StarNestSkyComponent_GetFormuparam(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Formuparam;
}

static void StarNestSkyComponent_SetFormuparam(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Formuparam = value;
}

static float StarNestSkyComponent_GetStepSize(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_StepSize;
}

static void StarNestSkyComponent_SetStepSize(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_StepSize = value;
}

static float StarNestSkyComponent_GetTile(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Tile;
}

static void StarNestSkyComponent_SetTile(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Tile = value;
}

static float StarNestSkyComponent_GetBrightness(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Brightness;
}

static void StarNestSkyComponent_SetBrightness(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Brightness = value;
}

static float StarNestSkyComponent_GetDarkMatter(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_DarkMatter;
}

static void StarNestSkyComponent_SetDarkMatter(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_DarkMatter = value;
}

static float StarNestSkyComponent_GetDistFading(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_DistFading;
}

static void StarNestSkyComponent_SetDistFading(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_DistFading = value;
}

static float StarNestSkyComponent_GetSaturation(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Saturation;
}

static void StarNestSkyComponent_SetSaturation(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Saturation = value;
}

static float StarNestSkyComponent_GetIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Intensity;
}

static void StarNestSkyComponent_SetIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Intensity = value;
}

static int StarNestSkyComponent_GetIterations(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_Iterations;
}

static void StarNestSkyComponent_SetIterations(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_Iterations = value;
}

static int StarNestSkyComponent_GetVolSteps(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_VolSteps;
}

static void StarNestSkyComponent_SetVolSteps(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_VolSteps = value;
}

static bool StarNestSkyComponent_GetEnableSkybox(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_EnableSkybox;
}

static void StarNestSkyComponent_SetEnableSkybox(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_EnableSkybox = value;
}

static bool StarNestSkyComponent_GetEnableIBL(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_EnableIBL;
}

static void StarNestSkyComponent_SetEnableIBL(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_EnableIBL = value;
}

static float StarNestSkyComponent_GetIBLIntensity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    return comp.m_IBLIntensity;
}

static void StarNestSkyComponent_SetIBLIntensity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StarNestSkyComponent>();
    comp.m_IBLIntensity = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// StreamingVolumeComponent                                                       //
///////////////////////////////////////////////////////////////////////////////////////////

static float StreamingVolumeComponent_GetLoadRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StreamingVolumeComponent>();
    return comp.LoadRadius;
}

static void StreamingVolumeComponent_SetLoadRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StreamingVolumeComponent>();
    comp.LoadRadius = value;
}

static float StreamingVolumeComponent_GetUnloadRadius(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<StreamingVolumeComponent>();
    return comp.UnloadRadius;
}

static void StreamingVolumeComponent_SetUnloadRadius(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<StreamingVolumeComponent>();
    comp.UnloadRadius = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// TerrainComponent                                                               //
///////////////////////////////////////////////////////////////////////////////////////////

static float TerrainComponent_GetWorldSizeX(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_WorldSizeX;
}

static void TerrainComponent_SetWorldSizeX(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_WorldSizeX = value;
}

static float TerrainComponent_GetWorldSizeZ(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_WorldSizeZ;
}

static void TerrainComponent_SetWorldSizeZ(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_WorldSizeZ = value;
}

static float TerrainComponent_GetHeightScale(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightScale;
}

static void TerrainComponent_SetHeightScale(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightScale = value;
}

static bool TerrainComponent_GetProceduralEnabled(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralEnabled;
}

static void TerrainComponent_SetProceduralEnabled(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralEnabled = value;
}

static int TerrainComponent_GetSeed(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralSeed;
}

static void TerrainComponent_SetSeed(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralSeed = value;
}

static unsigned int TerrainComponent_GetResolution(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralResolution;
}

static void TerrainComponent_SetResolution(UUID entityID, unsigned int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralResolution = value;
}

static unsigned int TerrainComponent_GetOctaves(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralOctaves;
}

static void TerrainComponent_SetOctaves(UUID entityID, unsigned int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralOctaves = value;
}

static float TerrainComponent_GetFrequency(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralFrequency;
}

static void TerrainComponent_SetFrequency(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralFrequency = value;
}

static float TerrainComponent_GetLacunarity(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralLacunarity;
}

static void TerrainComponent_SetLacunarity(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralLacunarity = value;
}

static float TerrainComponent_GetPersistence(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_ProceduralPersistence;
}

static void TerrainComponent_SetPersistence(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_ProceduralPersistence = value;
}

static float TerrainComponent_GetRidgeBlend(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightShaping.RidgeBlend;
}

static void TerrainComponent_SetRidgeBlend(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightShaping.RidgeBlend = value;
}

static float TerrainComponent_GetWarpStrength(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightShaping.WarpStrength;
}

static void TerrainComponent_SetWarpStrength(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightShaping.WarpStrength = value;
}

static float TerrainComponent_GetWarpFrequency(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightShaping.WarpFrequency;
}

static void TerrainComponent_SetWarpFrequency(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightShaping.WarpFrequency = value;
}

static unsigned int TerrainComponent_GetTerraceSteps(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightShaping.TerraceSteps;
}

static void TerrainComponent_SetTerraceSteps(UUID entityID, unsigned int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightShaping.TerraceSteps = value;
}

static float TerrainComponent_GetTerraceSharpness(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightShaping.TerraceSharpness;
}

static void TerrainComponent_SetTerraceSharpness(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightShaping.TerraceSharpness = value;
}

static float TerrainComponent_GetHeightExponent(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_HeightShaping.HeightExponent;
}

static void TerrainComponent_SetHeightExponent(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_HeightShaping.HeightExponent = value;
}

static bool TerrainComponent_GetAutoMaterial(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_AutoMaterial;
}

static void TerrainComponent_SetAutoMaterial(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_AutoMaterial = value;
}

static unsigned int TerrainComponent_GetSplatmapGenResolution(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    return comp.m_SplatmapGenResolution;
}

static void TerrainComponent_SetSplatmapGenResolution(UUID entityID, unsigned int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TerrainComponent>();
    comp.m_SplatmapGenResolution = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// TextComponent                                                                  //
///////////////////////////////////////////////////////////////////////////////////////////

static MonoString* TextComponent_GetText(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TextComponent>();
    return ScriptEngine::CreateString(comp.TextString.c_str());
}

static void TextComponent_SetText(UUID entityID, MonoString* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!value)
        return;
    auto& comp = entity.GetComponent<TextComponent>();
    comp.TextString = Utils::MonoStringToString(value);
}

static void TextComponent_GetColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TextComponent>();
    *outValue = comp.Color;
}

static void TextComponent_SetColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TextComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Color = *value;
}

static float TextComponent_GetKerning(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TextComponent>();
    return comp.Kerning;
}

static void TextComponent_SetKerning(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TextComponent>();
    comp.Kerning = value;
}

static float TextComponent_GetLineSpacing(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TextComponent>();
    return comp.LineSpacing;
}

static void TextComponent_SetLineSpacing(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<TextComponent>();
    comp.LineSpacing = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// TransformComponent                                                             //
///////////////////////////////////////////////////////////////////////////////////////////

static void TransformComponent_GetTranslation(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TransformComponent>();
    *outValue = comp.Translation;
}

static void TransformComponent_SetTranslation(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Translation = *value;
}

static void TransformComponent_GetScale(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TransformComponent>();
    *outValue = comp.Scale;
}

static void TransformComponent_SetScale(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.Scale = *value;
}

static void TransformComponent_GetRotation(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TransformComponent>();
    *outValue = comp.GetRotationEuler();
}

static void TransformComponent_SetRotation(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<TransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.SetRotationEuler(*value);
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIButtonComponent                                                              //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIButtonComponent_GetNormalColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    *outValue = comp.m_NormalColor;
}

static void UIButtonComponent_SetNormalColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_NormalColor = *value;
}

static void UIButtonComponent_GetHoveredColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    *outValue = comp.m_HoveredColor;
}

static void UIButtonComponent_SetHoveredColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_HoveredColor = *value;
}

static void UIButtonComponent_GetPressedColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    *outValue = comp.m_PressedColor;
}

static void UIButtonComponent_SetPressedColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_PressedColor = *value;
}

static void UIButtonComponent_GetDisabledColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    *outValue = comp.m_DisabledColor;
}

static void UIButtonComponent_SetDisabledColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_DisabledColor = *value;
}

static bool UIButtonComponent_GetInteractable(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    return comp.m_Interactable;
}

static void UIButtonComponent_SetInteractable(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    comp.m_Interactable = value;
}

static int UIButtonComponent_GetState(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    return static_cast<int>(comp.m_State);
}

static void UIButtonComponent_SetState(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIButtonComponent>();
    comp.m_State = static_cast<UIButtonState>(value);
}

///////////////////////////////////////////////////////////////////////////////////////////
// UICanvasComponent                                                              //
///////////////////////////////////////////////////////////////////////////////////////////

static int UICanvasComponent_GetSortOrder(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UICanvasComponent>();
    return comp.m_SortOrder;
}

static void UICanvasComponent_SetSortOrder(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UICanvasComponent>();
    comp.m_SortOrder = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UICheckboxComponent                                                            //
///////////////////////////////////////////////////////////////////////////////////////////

static bool UICheckboxComponent_GetIsChecked(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UICheckboxComponent>();
    return comp.m_IsChecked;
}

static void UICheckboxComponent_SetIsChecked(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UICheckboxComponent>();
    comp.m_IsChecked = value;
}

static bool UICheckboxComponent_GetInteractable(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UICheckboxComponent>();
    return comp.m_Interactable;
}

static void UICheckboxComponent_SetInteractable(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UICheckboxComponent>();
    comp.m_Interactable = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIDropdownComponent                                                            //
///////////////////////////////////////////////////////////////////////////////////////////

static int UIDropdownComponent_GetSelectedIndex(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIDropdownComponent>();
    return comp.m_SelectedIndex;
}

static void UIDropdownComponent_SetSelectedIndex(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIDropdownComponent>();
    comp.m_SelectedIndex = value;
}

static bool UIDropdownComponent_GetInteractable(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIDropdownComponent>();
    return comp.m_Interactable;
}

static void UIDropdownComponent_SetInteractable(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIDropdownComponent>();
    comp.m_Interactable = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIGridLayoutComponent                                                          //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIGridLayoutComponent_GetCellSize(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIGridLayoutComponent>();
    *outValue = comp.m_CellSize;
}

static void UIGridLayoutComponent_SetCellSize(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIGridLayoutComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_CellSize = *value;
}

static void UIGridLayoutComponent_GetSpacing(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIGridLayoutComponent>();
    *outValue = comp.m_Spacing;
}

static void UIGridLayoutComponent_SetSpacing(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIGridLayoutComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Spacing = *value;
}

static int UIGridLayoutComponent_GetConstraintCount(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIGridLayoutComponent>();
    return comp.m_ConstraintCount;
}

static void UIGridLayoutComponent_SetConstraintCount(UUID entityID, int value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIGridLayoutComponent>();
    comp.m_ConstraintCount = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIImageComponent                                                               //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIImageComponent_GetColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIImageComponent>();
    *outValue = comp.m_Color;
}

static void UIImageComponent_SetColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIImageComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Color = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIInputFieldComponent                                                          //
///////////////////////////////////////////////////////////////////////////////////////////

static MonoString* UIInputFieldComponent_GetText(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    return ScriptEngine::CreateString(comp.m_Text.c_str());
}

static void UIInputFieldComponent_SetText(UUID entityID, MonoString* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!value)
        return;
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    comp.m_Text = Utils::MonoStringToString(value);
}

static MonoString* UIInputFieldComponent_GetPlaceholder(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    return ScriptEngine::CreateString(comp.m_Placeholder.c_str());
}

static void UIInputFieldComponent_SetPlaceholder(UUID entityID, MonoString* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!value)
        return;
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    comp.m_Placeholder = Utils::MonoStringToString(value);
}

static float UIInputFieldComponent_GetFontSize(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    return comp.m_FontSize;
}

static void UIInputFieldComponent_SetFontSize(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    comp.m_FontSize = value;
}

static void UIInputFieldComponent_GetTextColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    *outValue = comp.m_TextColor;
}

static void UIInputFieldComponent_SetTextColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_TextColor = *value;
}

static bool UIInputFieldComponent_GetInteractable(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    return comp.m_Interactable;
}

static void UIInputFieldComponent_SetInteractable(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIInputFieldComponent>();
    comp.m_Interactable = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIPanelComponent                                                               //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIPanelComponent_GetBackgroundColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIPanelComponent>();
    *outValue = comp.m_BackgroundColor;
}

static void UIPanelComponent_SetBackgroundColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIPanelComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_BackgroundColor = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIProgressBarComponent                                                         //
///////////////////////////////////////////////////////////////////////////////////////////

static float UIProgressBarComponent_GetValue(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIProgressBarComponent>();
    return comp.m_Value;
}

static void UIProgressBarComponent_SetValue(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UIProgressBarComponent>();
    comp.m_Value = value;
}

static float UIProgressBarComponent_GetMinValue(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIProgressBarComponent>();
    return comp.m_MinValue;
}

static void UIProgressBarComponent_SetMinValue(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UIProgressBarComponent>();
    comp.m_MinValue = value;
}

static float UIProgressBarComponent_GetMaxValue(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIProgressBarComponent>();
    return comp.m_MaxValue;
}

static void UIProgressBarComponent_SetMaxValue(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UIProgressBarComponent>();
    comp.m_MaxValue = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIRectTransformComponent                                                       //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIRectTransformComponent_GetAnchorMin(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    *outValue = comp.m_AnchorMin;
}

static void UIRectTransformComponent_SetAnchorMin(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_AnchorMin = *value;
}

static void UIRectTransformComponent_GetAnchorMax(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    *outValue = comp.m_AnchorMax;
}

static void UIRectTransformComponent_SetAnchorMax(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_AnchorMax = *value;
}

static void UIRectTransformComponent_GetAnchoredPosition(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    *outValue = comp.m_AnchoredPosition;
}

static void UIRectTransformComponent_SetAnchoredPosition(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_AnchoredPosition = *value;
}

static void UIRectTransformComponent_GetSizeDelta(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    *outValue = comp.m_SizeDelta;
}

static void UIRectTransformComponent_SetSizeDelta(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_SizeDelta = *value;
}

static void UIRectTransformComponent_GetPivot(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    *outValue = comp.m_Pivot;
}

static void UIRectTransformComponent_SetPivot(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Pivot = *value;
}

static float UIRectTransformComponent_GetRotation(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    return comp.m_Rotation;
}

static void UIRectTransformComponent_SetRotation(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    comp.m_Rotation = value;
}

static void UIRectTransformComponent_GetScale(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    *outValue = comp.m_Scale;
}

static void UIRectTransformComponent_SetScale(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIRectTransformComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Scale = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIScrollViewComponent                                                          //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIScrollViewComponent_GetScrollPosition(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIScrollViewComponent>();
    *outValue = comp.m_ScrollPosition;
}

static void UIScrollViewComponent_SetScrollPosition(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIScrollViewComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_ScrollPosition = *value;
}

static void UIScrollViewComponent_GetContentSize(UUID entityID, glm::vec2* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIScrollViewComponent>();
    *outValue = comp.m_ContentSize;
}

static void UIScrollViewComponent_SetContentSize(UUID entityID, glm::vec2 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIScrollViewComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_ContentSize = *value;
}

static float UIScrollViewComponent_GetScrollSpeed(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIScrollViewComponent>();
    return comp.m_ScrollSpeed;
}

static void UIScrollViewComponent_SetScrollSpeed(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UIScrollViewComponent>();
    comp.m_ScrollSpeed = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UISliderComponent                                                              //
///////////////////////////////////////////////////////////////////////////////////////////

static float UISliderComponent_GetValue(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UISliderComponent>();
    return comp.m_Value;
}

static void UISliderComponent_SetValue(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UISliderComponent>();
    comp.m_Value = value;
}

static float UISliderComponent_GetMinValue(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UISliderComponent>();
    return comp.m_MinValue;
}

static void UISliderComponent_SetMinValue(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UISliderComponent>();
    comp.m_MinValue = value;
}

static float UISliderComponent_GetMaxValue(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UISliderComponent>();
    return comp.m_MaxValue;
}

static void UISliderComponent_SetMaxValue(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UISliderComponent>();
    comp.m_MaxValue = value;
}

static bool UISliderComponent_GetInteractable(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UISliderComponent>();
    return comp.m_Interactable;
}

static void UISliderComponent_SetInteractable(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UISliderComponent>();
    comp.m_Interactable = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UITextComponent                                                                //
///////////////////////////////////////////////////////////////////////////////////////////

static MonoString* UITextComponent_GetText(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UITextComponent>();
    return ScriptEngine::CreateString(comp.m_Text.c_str());
}

static void UITextComponent_SetText(UUID entityID, MonoString* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!value)
        return;
    auto& comp = entity.GetComponent<UITextComponent>();
    comp.m_Text = Utils::MonoStringToString(value);
}

static float UITextComponent_GetFontSize(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UITextComponent>();
    return comp.m_FontSize;
}

static void UITextComponent_SetFontSize(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UITextComponent>();
    comp.m_FontSize = value;
}

static void UITextComponent_GetColor(UUID entityID, glm::vec4* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UITextComponent>();
    *outValue = comp.m_Color;
}

static void UITextComponent_SetColor(UUID entityID, glm::vec4 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UITextComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_Color = *value;
}

static float UITextComponent_GetKerning(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UITextComponent>();
    return comp.m_Kerning;
}

static void UITextComponent_SetKerning(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UITextComponent>();
    comp.m_Kerning = value;
}

static float UITextComponent_GetLineSpacing(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UITextComponent>();
    return comp.m_LineSpacing;
}

static void UITextComponent_SetLineSpacing(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<UITextComponent>();
    comp.m_LineSpacing = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIToggleComponent                                                              //
///////////////////////////////////////////////////////////////////////////////////////////

static bool UIToggleComponent_GetIsOn(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIToggleComponent>();
    return comp.m_IsOn;
}

static void UIToggleComponent_SetIsOn(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIToggleComponent>();
    comp.m_IsOn = value;
}

static bool UIToggleComponent_GetInteractable(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIToggleComponent>();
    return comp.m_Interactable;
}

static void UIToggleComponent_SetInteractable(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIToggleComponent>();
    comp.m_Interactable = value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// UIWorldAnchorComponent                                                         //
///////////////////////////////////////////////////////////////////////////////////////////

static void UIWorldAnchorComponent_GetWorldOffset(UUID entityID, glm::vec3* outValue)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIWorldAnchorComponent>();
    *outValue = comp.m_WorldOffset;
}

static void UIWorldAnchorComponent_SetWorldOffset(UUID entityID, glm::vec3 const* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<UIWorldAnchorComponent>();
    for (glm::length_t i = 0; i < value->length(); ++i)
        if (!std::isfinite((*value)[i]))
            return;
    comp.m_WorldOffset = *value;
}

///////////////////////////////////////////////////////////////////////////////////////////
// VideoOverlayComponent                                                          //
///////////////////////////////////////////////////////////////////////////////////////////

static bool VideoOverlayComponent_GetPlayOnStart(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    return comp.PlayOnStart;
}

static void VideoOverlayComponent_SetPlayOnStart(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    comp.PlayOnStart = value;
}

static bool VideoOverlayComponent_GetSkipOnInput(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    return comp.SkipOnInput;
}

static void VideoOverlayComponent_SetSkipOnInput(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    comp.SkipOnInput = value;
}

static bool VideoOverlayComponent_GetLooping(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    return comp.Looping;
}

static void VideoOverlayComponent_SetLooping(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    comp.Looping = value;
}

static float VideoOverlayComponent_GetVolume(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    return comp.Volume;
}

static void VideoOverlayComponent_SetVolume(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    comp.Volume = value;
}

static MonoString* VideoOverlayComponent_GetVideoPath(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    return ScriptEngine::CreateString((comp.VideoPath).c_str());
}

static void VideoOverlayComponent_SetVideoPath(UUID entityID, MonoString* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!value)
        return;
    auto& comp = entity.GetComponent<VideoOverlayComponent>();
    std::string converted = Utils::MonoStringToString(value);
    comp.VideoPath = converted;
}

///////////////////////////////////////////////////////////////////////////////////////////
// VideoSurfaceComponent                                                          //
///////////////////////////////////////////////////////////////////////////////////////////

static bool VideoSurfaceComponent_GetAutoPlay(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    return comp.AutoPlay;
}

static void VideoSurfaceComponent_SetAutoPlay(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    comp.AutoPlay = value;
}

static bool VideoSurfaceComponent_GetLooping(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    return comp.Looping;
}

static void VideoSurfaceComponent_SetLooping(UUID entityID, bool value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    comp.Looping = value;
}

static float VideoSurfaceComponent_GetVolume(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    return comp.Volume;
}

static void VideoSurfaceComponent_SetVolume(UUID entityID, float value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!std::isfinite(value))
        return;
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    comp.Volume = value;
}

static MonoString* VideoSurfaceComponent_GetVideoPath(UUID entityID)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    return ScriptEngine::CreateString((comp.VideoPath).c_str());
}

static void VideoSurfaceComponent_SetVideoPath(UUID entityID, MonoString* value)
{
    Scene* scene = ScriptEngine::GetSceneContext();
    OLO_CORE_ASSERT(scene);
    Entity entity = scene->GetEntityByUUID(entityID);
    OLO_CORE_ASSERT(entity);
    if (!value)
        return;
    auto& comp = entity.GetComponent<VideoSurfaceComponent>();
    std::string converted = Utils::MonoStringToString(value);
    comp.VideoPath = converted;
}
