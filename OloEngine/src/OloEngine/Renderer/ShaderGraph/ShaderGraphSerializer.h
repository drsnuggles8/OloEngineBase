#pragma once

// ShaderGraphSerializer is declared in AssetSerializer.h alongside all other asset serializers.
// This header provides a convenient include for code that specifically needs the shader graph serializer.
#include "OloEngine/Asset/AssetSerializer.h"
