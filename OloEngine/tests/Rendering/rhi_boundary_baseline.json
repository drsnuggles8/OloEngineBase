{
  "_comment": [
    "Phase 2 ratchet baseline for issue #691 (RHI: Vulkan backend alongside OpenGL 4.6).",
    "",
    "THESE NUMBERS MAY ONLY EVER GO DOWN. RHIBoundaryRatchetTest fails if any",
    "counter rises above its baseline. When you lower one, edit the number here in",
    "the same commit — the test prints the exact value to paste.",
    "",
    "Do NOT raise a baseline to make the build green. A rise means new raw OpenGL",
    "was added outside Platform/OpenGL/, which is the regression the ratchet",
    "exists to catch. Route it through the RendererAPI facade instead; if that",
    "genuinely cannot express it, that is a Phase 2 design gap worth a comment on",
    "#691, not a baseline bump.",
    "",
    "Counting rule (implemented in RHIBoundaryRatchetTest.cpp, must match this):",
    "an identifier matching gl[A-Z][A-Za-z0-9_]* immediately followed by '(',",
    "counted AFTER C/C++ comments and string/char literal bodies are stripped.",
    "Stripping matters less than pattern precision: with the same pattern it",
    "removes only 16 of 565 hits, whereas a pattern loose enough to match glfw*",
    "is what produced the task handover's '724' (the issue body's '~620' was a",
    "rough estimate). Both are done here so the number a test asserts on is exact.",
    "",
    "Buckets and what they mean — see ADR 0011 sections 0, 1.6 and 1.7:",
    "  sweep  = OloEngine/src/** minus Platform/OpenGL/ minus Renderer/Debug/.",
    "           Phase 2 target. MUST reach zero.",
    "  tools  = OloEngine/src/OloEngine/Renderer/Debug/. Phase 8 target. These",
    "           are RELOCATED behind a neutral interface into Platform/OpenGL/,",
    "           not exempted: CLAUDE.md's rendering-verification rule is enforced",
    "           through these tools and the MCP capture endpoints they back, so a",
    "           Vulkan backend they cannot see is one Phase 7 cannot verify.",
    "  Platform/OpenGL/ is the backend and is not counted at all.",
    "",
    "sweep_glad_includes is the counter that actually PROVES the property: a",
    "translation unit that cannot see <glad/gl.h> provably cannot name a GL",
    "symbol, whereas a call count can be gamed by a helper that still lives in",
    "Renderer/. It is not yet meaningful, though, because RendererAPI.h itself",
    "includes <glad/gl.h> and every pass includes RendererAPI.h — so the symbols",
    "arrive transitively and removing a per-file include changes nothing. Strip",
    "the GLenum/GLuint parameters out of RendererAPI's virtuals FIRST (ADR 0011",
    "section 1.7); only then does each removed include become a real guarantee.",
    "",
    "debug_escape_hatch is uses of RHI::GetNativeHandleForDebug outside",
    "Renderer/Debug/ and Platform/. It starts at zero and must stay there: the",
    "whole point of that spelling is that it is conspicuous in review.",
    "",
    "PHASE 2 STEP 1 (2026-07-30): sweep_glad_includes 70 -> 39. RendererAPI's",
    "GLenum/GLuint/GLint virtuals are gone, so RendererAPI.h no longer includes",
    "<glad/gl.h> and neither do the two RenderCommand facades — which makes every",
    "per-file include removal a real, compiler-enforced guarantee for the first",
    "time (ADR 0011 section 1.7's ordering constraint, now satisfied).",
    "",
    "sweep_gl_calls deliberately did NOT move: step 1 converts the facade's",
    "vocabulary, step 2 sweeps the raw call sites. A drop here would have meant",
    "calls were hidden rather than removed.",
    "",
    "The 39 is 34 removals against 3 ADDITIONS, and the additions are correct:",
    "Renderer3DLifecycle.cpp, UIRenderer.cpp and Scene.cpp call GL but had no",
    "direct include, relying on RendererAPI.h's transitive one (ADR 0011 section 0",
    "predicted exactly 3 such files). Closing that path makes them name their own",
    "dependency. Do NOT 'fix' that bump by re-hiding them behind a transitive",
    "include — they are step-2 sweep backlog, and the counter is now honest.",
    "",
    "Two transitive sources were found, not one. The second was wider and is the",
    "reason this counter could have read zero while proving nothing:",
    "Debug/Instrumentor.h included <tracy/TracyOpenGL.hpp>, and Instrumentor.h is",
    "in OloEnginePCH.h — so every TU in the engine saw the whole GL API. The",
    "OLO_PROFILE_GPU* macros it served were used nowhere, so the include was",
    "deleted. A per-file #include scan cannot see a PCH; check it by hand.",
    "",
    "PHASE 2 STEP 2 (2026-07-30): sweep_gl_calls 313 -> 0 and",
    "sweep_glad_includes 39 -> 0. The sweep bucket now contains no raw OpenGL",
    "call and no translation unit in it can name a GL symbol at all. Phase 2's",
    "headline target is met.",
    "",
    "BOTH ZEROES ARE NOW FLOORS, NOT TARGETS. A rise in either is a real",
    "regression with no remaining excuse: the facade gained ~60 virtuals in this",
    "step precisely so every operation the engine performs has a neutral",
    "spelling (buffer binding points, buffer/framebuffer/VAO/query/fence",
    "lifecycle, named-framebuffer draw+read attachment selection, blits, texture",
    "clear/upload/readback, debug markers, device idle, MSAA caps). If you find",
    "an operation with no facade entry point, add the virtual — do not reach for",
    "glad.",
    "",
    "Three things this step is worth remembering for:",
    "",
    "1. The counter reaching zero was NOT the end of the work. A Platform/OpenGL",
    "   header included from the sweep bucket leaks the whole GL API just as",
    "   effectively as <glad/gl.h>, and this scan cannot see it. Three passes",
    "   (FluidIntermediatesPass, WaterRenderPass, VirtualMeshRegistry) included",
    "   Platform/OpenGL/OpenGLUtilities.h for GLClearProgramGuard. The fix was to",
    "   move the guard INTO the backend clear implementations where it belongs —",
    "   not to delete the glad include and call it done, which would have zeroed",
    "   this counter while proving nothing. Check for Platform/<Backend>/ includes",
    "   by hand, exactly like the PCH.",
    "",
    "2. Zero GL CALLS does not mean zero GL. UIRenderer.cpp had no calls but typed",
    "   its clip-rect stack in GLint/GLsizei, so it genuinely could not compile",
    "   without the header. Removing an include is only safe when the file names",
    "   no GL* identifier of any kind — and only a compile proves it.",
    "",
    "3. A loader-symbol probe is invisible here by construction. RGCommandContext",
    "   guarded its debug markers with `if (GLAD_GL_KHR_debug)`, which does not",
    "   match gl[A-Z] and so never appeared in this count. The capability check",
    "   moved into OpenGLRendererAPI::PushDebugGroup. Same class as step 1's",
    "   `glad_glCreateTextures != nullptr` context probe.",
    "",
    "PHASE 2 STEP 3, PART 1 (2026-07-31): the identity currency gets a MINT, and",
    "three counters to sweep onto it. Steps 1 and 2 removed GL's vocabulary and",
    "GL's calls; step 3 removes GL's CURRENCY — the u32 `GetRendererID()` that ADR",
    "0011 section 1.1 identifies as one integer conflating object identity, driver",
    "handle and binding address.",
    "",
    "WHAT LANDED: Renderer/RHI/RHIResourceRegistry — the generation-checked",
    "producer of RHI::ResourceHandle, with contract tests. Phase 1 declared the",
    "handle and specified what it means; nothing minted one, and",
    "GetNativeHandleForDebug was declared and defined nowhere. Both are now real.",
    "",
    "WHAT DID NOT LAND: the call-site conversion onto it. THE COUNTERS BELOW ARE",
    "THEREFORE NOT ZERO, and that is deliberate and honest — they measure the work",
    "remaining, exactly as sweep_gl_calls started at 313 and sweep_glad_includes",
    "at 70. This mirrors how Phase 1 shipped: a vocabulary plus a ratchet, nothing",
    "converted yet.",
    "",
    "Three new counters, all monotonically non-increasing, all targeting zero:",
    "WHICH COUNTER MEANS WHAT — corrected once the first real migration exposed it:",
    "sweep_renderer_id UNDERCOUNTS. It measures the ACCESSOR NAME, not the",
    "currency. A pass holding `u32 m_NoiseTexture` from CreateTexture2D contains",
    "no `RendererID` token at all and is nonetheless entirely unmigrated —",
    "SSAORenderPass has 2 RendererID mentions against 5 native texture ids.",
    "Driving it to zero proves 'nobody calls GetRendererID', NOT 'no native ids",
    "flow through the engine'.",
    "",
    "facade_native_id_params IS THE COMPLETION CRITERION. When the u32 facade",
    "forms are deleted, no caller CAN hold a native id — the type system enforces",
    "it, which is the same 'provable rather than measured' property section 1.7",
    "credits sweep_glad_includes with. Treat sweep_renderer_id as a progress",
    "proxy and facade_native_id_params as the proof.",
    "",
    "  sweep_renderer_id      = mentions of the identifier `RendererID` in any",
    "                           spelling (GetRendererID, the `using RendererID =",
    "                           u32` alias, the backend's m_RendererID member)",
    "                           outside Platform/ and Renderer/Debug/. A native",
    "                           object name may not be NAMED there, let alone held.",
    "                           699 across 79 files; the heaviest are",
    "                           Renderer3DMeshSubmission.cpp (70),",
    "                           Commands/RenderCommand.h (68),",
    "                           Commands/CommandDispatch.cpp (67), Scene.cpp (65).",
    "  facade_native_id_params= parameters in RendererAPI.h declared `u32 <name>ID`.",
    "                           This one goes to zero LAST, not first, and the",
    "                           reason is an asymmetry worth internalising:",
    "                           handle -> native is a registry lookup, but",
    "                           native -> handle is NOT RECOVERABLE (a driver name",
    "                           does not identify a slot). So a facade that takes",
    "                           handles cannot serve a caller still holding a u32,",
    "                           whereas a facade offering BOTH can serve either.",
    "                           Order is therefore: add handle overloads (backends",
    "                           resolve internally), migrate callers subsystem by",
    "                           subsystem, then delete the u32 forms — at which",
    "                           point this counter drops to zero in one step and",
    "                           the boundary becomes compiler-enforced.",
    "                           (An earlier note here said 'drive it to zero",
    "                           FIRST'. That was reasoned from an all-at-once",
    "                           conversion, where flipping the facade forces every",
    "                           call site at once — which is exactly the",
    "                           unreviewable change this plan avoids.)",
    "  backend_resolve_hatch  = uses of ResourceRegistry::ResolveNativeForBackend",
    "                           outside Platform/. Sibling of debug_escape_hatch:",
    "                           two conspicuously-named hatches out of a handle,",
    "                           each baselined at zero where it does not belong.",
    "                           Renderer/RHI/ is excluded because it declares and",
    "                           defines the function. Starts at zero and must stay",
    "                           there — the sweep must not smuggle native names out.",
    "",
    "THE COUNTERS ARE A PROGRESS MEASURE, NOT THE PROOF, and it is worth being",
    "precise about why. A count of `RendererID` mentions is gameable — rename the",
    "accessor and it reads zero while a native GL name still crosses the boundary.",
    "What is NOT gameable is the type system: there is no implicit conversion",
    "between RHI::ResourceHandle and an integer in either direction, so once the",
    "facade takes handles, a translation unit holding a native name cannot call",
    "it. (Handle is an aggregate, so C++20 parenthesized aggregate init makes the",
    "EXPLICIT spelling ResourceHandle(someId) well-formed — a narrow, conspicuous",
    "hole that is self-defusing because the result carries Generation == 0 and so",
    "fails IsValid() and resolves to 0. Both facts are asserted in",
    "ResourceHandleCannotBeSpelledAsANativeId, which found this by asserting the",
    "opposite and failing to compile.)",
    "That is the same 'provable rather than measured' property section 1.7 credits",
    "sweep_glad_includes with, one level up. ResourceHandleCannotBeSpelledAsANativeId",
    "static_asserts the non-convertibility both ways, so a well-meant `Handle(u32)`",
    "convenience constructor cannot silently reopen the boundary before the sweep",
    "even starts.",
    "",
    "TWO TRAPS FOUND BY AN ABANDONED FULL-SWEEP ATTEMPT, recorded because they cost",
    "real time and neither is obvious (the full account, including three",
    "scripted-edit defect classes, is in",
    "docs/agent-rules/rhi-abstraction-boundary.md section 4):",
    "",
    "1. A `.data()` boundary is where the type system stops helping. CreateQueries",
    "   passes `std::span<u32>::data()` to glCreateQueries; re-typing that span to",
    "   std::span<RHI::ResourceHandle> KEEPS COMPILING and has GL write 4-byte names",
    "   over an array of 8-byte handles. Grep for .data() on any container whose",
    "   element type changes.",
    "",
    "2. Every Delete* virtual needs TWO changes, and only one is obvious: resolve",
    "   the handle to call glDelete*, AND unregister it. Skip the second and the",
    "   slot keeps its generation, so a handle to a destroyed object goes on",
    "   resolving to a name the driver may reissue — the recycled-name failure the",
    "   whole layer exists to prevent, reintroduced at the site most likely to be",
    "   treated as mechanical.",
    "",
    "PHASE 2 STEP 3, SLICE 5 (2026-08-01): the framebuffer ATTACHMENT consumers.",
    "sweep_renderer_id 699 -> 653. facade_native_id_params deliberately UNCHANGED",
    "at 68: this slice adds handle overloads and deletes no u32 form, which is the",
    "documented order (add overloads, migrate callers, delete the u32 forms last).",
    "",
    "Migrated: all seven bakers, the straightforward bind passes (Fog, Overdraw,",
    "SelectionOutline, Cloudscape composite, Decal, Bloom), every attachment read",
    "in DDGIProbeUpdatePass, and RenderGraph's attachment clear + NaN census.",
    "Eight sites were deliberately left native; the reason for each is tabulated in",
    "docs/agent-rules/rhi-abstraction-boundary.md ('What slice 5 actually moved').",
    "",
    "SIX NEW FACADE VIRTUALS were needed, against a worklist that predicted zero:",
    "CopyImageSubData, CopyImageSubDataFull, ClearTextureFloat, the offset form of",
    "UploadTextureSubImage2D, ReadTextureImage, ReadTextureSubImage. The prediction",
    "was wrong because the survey behind it counted only BIND sinks, and the",
    "attachment getters also feed a copy family (bakers staging an attachment into a",
    "persistent texture) and a readback family (thumbnail / probe capture). Same",
    "lesson as the SetTextureFilter/SetTextureWrap/UploadTextureSubImage2D discovery",
    "one slice earlier: migrate one real consumer PER SINK FAMILY and let the",
    "compiler enumerate the rest.",
    "",
    "TWO SILENT DEFECTS FOUND, both worth knowing beyond this issue:",
    "",
    "1. Hashing a driver name into a cache fingerprint cannot see a",
    "   destroy-then-recreate. RenderPipeline hashed DDGI's atlas ids so a",
    "   Resolution/HitCacheTexels edit would rebuild the frame graph — but",
    "   EnsureResources calls DestroyResources() BEFORE creating the replacements,",
    "   so GL may reissue the same names and the fingerprint never changed. The",
    "   graph then kept an import whose Width/Height described the OLD resolution.",
    "   Now hashes RHI::HashKey(handle); pinned by RHIResourceRegistry's",
    "   HashKeyDiffersAcrossADestroyRecreateThatReusesTheNativeName. Note the",
    "   opposite teardown order (allocate-then-release) hides this completely, so",
    "   whether the bug is live depends on a line nowhere near the hash.",
    "",
    "2. ImportTextureHandle BLINDS RenderGraph::ResolveTexture, and that is what the",
    "   MCP capture endpoints read. textureID and identity are alternatives on a",
    "   PhysicalTexture, so a handle-imported resource resolves natively to 0 —",
    "   olo_render_list_targets reports id 0 and olo_render_capture_target cannot",
    "   find it. #732 already did this to SSAO's noise texture. Fixed with a",
    "   fallback through RHI::GetNativeHandleForDebug in OloEditor (which this",
    "   scanner does not walk, so debug_escape_hatch stays honestly 0 rather than",
    "   being waived). CONSEQUENCE FOR LATER SLICES: a resource's import may only",
    "   move to ImportTextureHandle after the diagnostics can read one — which is",
    "   why DDGI's importAtlas is still native here.",
    "",
    "PHASE 2 STEP 3, SLICE 6 (2026-08-01): the command-layer bind cache — the",
    "CURRENCY itself, not one producer family. sweep_renderer_id 653 -> 355 (six",
    "times slice 5's move); facade_native_id_params 68 -> 67.",
    "",
    "`using RendererID = u32` is DELETED. Every GPU-object field on the POD",
    "command structs, the redundant-bind cache (BoundTextures / BoundUBOs /",
    "CurrentBoundShader / CurrentBoundVAO / the six per-frame shadow fields),",
    "DepthPrepassShaderIDs, InstanceGroupKey and the draw sort keys are",
    "RHI::ResourceHandle now.",
    "",
    "THREE CORRECTNESS FIXES, not just a type change:",
    "  1. InstanceGroupKey batched draws by VAO GL NAME. A delete/create pair can",
    "     hand two objects the same name, merging unrelated draws into one batch —",
    "     one mesh rendered with another's geometry. Two live handles cannot",
    "     collide.",
    "  2. DepthPrepassShaderIDs compared PROGRAMS by GL name to decide whether a",
    "     material's shader could be swapped for the depth-only one. A relinked",
    "     program can inherit a freed name across a hot reload.",
    "  3. RenderPipeline's shadow and IBL fingerprints hashed raw ids. ShadowMap",
    "     ::SetSettings calls Shutdown() BEFORE Init() on a resolution change, so",
    "     GL may reissue the freed names and the hash would see no change — the",
    "     same defect the DDGI atlases had in slice 5.",
    "",
    "THE DOC CLAIM THIS SLICE FALSIFIED. rhi-abstraction-boundary.md said keying",
    "the cache on identities makes the Invalidate* calls 'a pure optimisation'.",
    "That is WRONG and wrong in the direction that ships bugs. The recycled-name",
    "collision does die — but an IN-PLACE RELOAD deliberately PRESERVES the",
    "identity while replacing the storage (ScopedResourceHandle::Sync never",
    "retires; that is what makes caching a handle safe). The cache then holds the",
    "very handle being rebound, concludes 'already bound', and SKIPS a bind that",
    "must happen — leaving the unit pointing at a deleted GL name. Under native-id",
    "keying that self-corrected because the name changed. So every site that",
    "recreates a texture's storage MUST call InvalidateTextureBinding. The doc is",
    "corrected in this commit.",
    "",
    "WHEN A MIGRATION SEEMS TO REQUIRE RAISING A RATCHET, THE CALL SITE IS USUALLY",
    "WRONG. DrawElementsIndirectRaw(vaoID, bufferID) needed a handle form, and the",
    "obvious mixed (handle, u32 bufferID) overload would have pushed",
    "facade_native_id_params to 69. Its ONE caller had already run BindVAOIfNeeded,",
    "so the draw was re-binding the VAO behind the redundant-bind cache's back;",
    "DrawBoundElementsIndirect(u32) replaced both u32 forms, removed the redundant",
    "bind, and netted -1 instead of +2.",
    "",
    "PHASE 2 STEP 3, SLICE 7 (2026-08-01): item 3's leftovers. 355 -> 347.",
    "",
    "All eight sites deferred by slice 5 as 'blocked on the transient pool' are",
    "migrated. THE BLOCKER WAS NEVER REAL, and how it got recorded as fact is the",
    "part worth keeping: TransientPool::AcquireTexture returns a Ref<Texture2D>,",
    "which has minted handles since slice 2. AcquiredInfo::RendererID is a",
    "diagnostics field with no role in resolution. What actually stood in the way",
    "was PhysicalTexture's documented invariant that TextureID and Handle are",
    "'ALTERNATIVES, exactly one is set' -- correct for an IMPORT (an importer only",
    "has one currency, and neither is derivable from the other), never true for a",
    "TRANSIENT (the planner holds the pooled Ref, so it has both and reads them",
    "off one pointer in one statement).",
    "",
    "So: when a migration says 'blocked on X', check whether X is a missing",
    "CAPABILITY or an INVARIANT someone wrote down. The first is work; the second",
    "is a decision that can be revisited once you know which case it was written",
    "for. Not checking cost a slice of imagined work and put a false claim on the",
    "issue tracker until it was corrected.",
    "",
    "Bonus: each of those sites guards its copy with `if (src != dst)`. Those now",
    "compare OBJECTS, so a recycled driver name can no longer make a source and",
    "its export look identical and skip a copy the frame needed -- the",
    "InstanceGroupKey defect class, in four more places.",
    "",
    "REVIEW PASS (2026-08-01): 347 -> 345, and the split is a live demonstration",
    "of the UNDERCOUNT warning above. Two fixes landed: Texture::operator== moved",
    "off GetRendererID (-2, the whole delta), and three sort keys in",
    "Renderer3DSpecializedDraws.cpp moved from cmd->shader->GetRendererID() to",
    "cmd->shaderRendererID.Index -- which nets ZERO, because the counter matches",
    "`RendererID` in any spelling and the field is still named shaderRendererID.",
    "The sort-key fix is the more consequential of the two (it stops sorting",
    "identity-keyed draws by a driver name the command no longer carries), and the",
    "proxy cannot see it at all. Do not read a flat counter as a flat slice.",
    "",
    "PHASE 2 STEP 3, ITEM 4 (2026-08-02): the native-id TAIL. STEP 3 IS COMPLETE.",
    "facade_native_id_params 67 -> 0; sweep_renderer_id 345 -> 168.",
    "",
    "RendererAPI.h now declares no `u32 <name>ID` parameter at all. That is the",
    "completion criterion, and it is the same kind of guarantee sweep_glad_includes",
    "gives one level down: a translation unit in the sweep bucket that holds a",
    "native name has nothing to pass it to, so the boundary is compiler-enforced",
    "rather than measured.",
    "",
    "THE PLAN SAID THIS COUNTER WOULD DRAIN PER ENTRY POINT. IT DID NOT, and the",
    "reason generalises. Each u32 form was supposed to go as its last caller",
    "migrated; in practice the last caller of nearly every form is another form's",
    "caller (ResolveTexture feeds BindTexture, GetColorAttachmentRendererID feeds",
    "CopyImageSubData, CreateTexture2D feeds SetTextureFilter), so the facade",
    "flipped in one edit and ~250 call sites were then repaired against the",
    "compiler's own (file, line) output. Budget a tail like this as ONE",
    "indivisible change, not a drain.",
    "",
    "QUERIES BECAME IDENTITIES -- the design question this item carried. Two",
    "reasons, one of them concrete: RHI::ResourceKind::Query already existed in the",
    "registry, so leaving them on u32 kept that enumerator dead; and",
    "OcclusionQueryPool hands a query issued in frame N to BeginConditionalRender",
    "in frame N+1, so a Shutdown()+Initialize() in between frees the names and GL",
    "may reissue one -- gating a draw on an unrelated occlusion result, which looks",
    "like a plausible frame rather than a broken one. A retired handle resolves to",
    "0 and the conditional render is skipped instead.",
    "",
    "CreateQueries is where the .data() trap recorded above applies LITERALLY (that",
    "warning was written about this function). The out-span never reaches the",
    "driver: the backend creates into its own std::vector<GLuint>, registers each",
    "name, and writes handles back. DeleteQueries resolves AND unregisters, with",
    "the same wrong-kind guard the other Delete* virtuals carry.",
    "IsQueryResultAvailable answers false for a stale handle rather than reading",
    "query 0 -- for occlusion a zero result reads as 'fully occluded', so the",
    "honest-looking answer is the one that deletes geometry.",
    "",
    "TWO SILENT DEFECTS, both the same shape, neither a type error, both found by",
    "reading rather than by a test:",
    "",
    "1. DeclareTransientTexture(name, desc, backingTextureID) set ONLY the native",
    "   id. That call publishes ShadowMapCSM / ShadowMapAtlas / their compare-off",
    "   raw views into the frame graph, and every consumer reads them through",
    "   ResolveTextureHandle -- which answers null for an entry with no Handle. So",
    "   DeferredLightingPass would have fallen through to the CSM placeholder every",
    "   frame: shadows gone, no error, no failing assertion.",
    "",
    "2. The transient planner's ALIAS FAN-OUT set only the native id. Pass 1",
    "   (WillAllocate) sets both since slice 7; pass 2 -- the entries that inherit a",
    "   sibling's physical resource under aliasing -- still set TextureID alone, so",
    "   the aliased half of any plan resolved to a null identity. Worse than (1)",
    "   because it is load-dependent: it appears only when the planner decides to",
    "   alias, which depends on the frame's resource lifetimes.",
    "",
    "THE RULE, and it corrects PhysicalTexture's own comment: 'TextureID and Handle",
    "are ALTERNATIVES, exactly one is set' is right for an IMPORT, where the",
    "importer holds one currency and neither is derivable from the other. It is",
    "wrong everywhere THE GRAPH ITSELF does the setting, because there it holds the",
    "resource object and reads both off one pointer in one statement. Slice 7",
    "learned this for the transient acquire and fixed that site; item 4 found two",
    "more governed by the same rule. Next time, grep EVERY assignment to .TextureID",
    "and check each for a matching .Handle, rather than fixing the one the current",
    "slice happens to touch.",
    "",
    "TWO WORKLIST 'BLOCKERS' WERE PHANTOMS, in opposite directions:",
    "  * VertexBuffer / IndexBuffer 'expose no GetRHIHandle()' -- true and",
    "    irrelevant. Their accessor is spelled GetBufferHandle() and is used only",
    "    inside Platform/OpenGL/; what the consumers actually hold is a",
    "    Ref<StorageBuffer>, which has minted handles since slice 2. The gap was",
    "    recorded by reasoning about the type family instead of grepping the",
    "    accessor the caller uses.",
    "  * 'An import may only move to ImportTextureHandle after the diagnostics can",
    "    read one' -- still true, and already satisfied. #736's",
    "    Debug::NativeTextureIdForDiagnostics unblocked six imports at once (DDGI's",
    "    atlases, the colour-grading LUT, the fluid intermediates, the fog volumes,",
    "    the TAA/clouds history, the virtual-geometry debug target). A sequencing",
    "    constraint that has been MET reads exactly like one still blocking.",
    "",
    "THE IBL TRIO'S SECOND CURRENCY DIED WITH ITS ONLY REASON TO EXIST. SetGlobalIBL",
    "took four identities AND three native ids; the natives existed for exactly",
    "three lines of RenderPipeline's graph.ImportTexture(...), because at the time",
    "the graph could not import an identity. Switching those to ImportTextureHandle",
    "removed the parameters, three Renderer3DData fields, three accessors, and the",
    "'both currencies clear together' comment that existed to keep them in step.",
    "When a value carries two currencies, find the single consumer that forced it.",
    "",
    "WHAT IS DELIBERATELY NOT DONE. GetRendererID() still exists on the resource",
    "classes -- it is what Platform/ and Renderer/Debug/ are supposed to use, and",
    "the graph's diagnostics path reads it on purpose. Item 4 removes every way for",
    "the SWEEP BUCKET to use one, not the accessor; deleting that is Phase 8, with",
    "the tools relocation. The creators keep their ...Handle suffix (slice 4 called",
    "it temporary; renaming ~40 call sites buys nothing and Phase 3 reshapes the",
    "create family anyway). shaderRendererID keeps its FIELD NAME on the POD command",
    "structs -- it has been an RHI::ResourceHandle since slice 6, and those 114",
    "mentions are the bulk of the remaining 168.",
    "",
    "PHASE 3 CONVERSION (2026-08-03): 232 -> 208. Eleven shaders and 24 sites are",
    "converted (SSAO, AOApply, FXAA, ChromaticAberration, ColorGrading,",
    "MotionBlur, TAA, ContactShadow, DOF, EASU, Fog). The remainder splits into",
    "~128 mechanical pass binds, ~21 material-path sites behind the redundant-bind",
    "cache, and 38 BindImageTexture (storage-image) sites the sampler-only heap",
    "does not model at all -- see ADR 0011 amendment (26). Keep the docs and this",
    "number in step: three passages quoted mid-conversion figures on the first",
    "push and CodeRabbit caught the drift.",
    "",
    "PHASE 3 (2026-08-02): THE BINDLESS REHEARSAL. A fourth counter,",
    "sweep_bind_texture_sites, and it is the first one here whose target is NOT",
    "zero. Read that difference before reading the number.",
    "",
    "  sweep_bind_texture_sites = occurrences of `BindTexture(` or",
    "                             `BindImageTexture(` outside Platform/ and",
    "                             Renderer/Debug/, counted after comments and",
    "                             string literals are blanked. Under heap-bindless",
    "                             a pass writes RHI::OffsetOf(view) into a UBO and",
    "                             the shader indexes the heap, so every one of",
    "                             these is a site the model deletes (ADR 0011",
    "                             section 1.3 item 1).",
    "",
    "WHY ZERO IS NOT THE TARGET, and why that is a design decision rather than a",
    "shortfall. ARB_bindless_texture is not universally available, so the",
    "slot-based path has to survive as the fallback on any device without it. A",
    "zero here would mean the engine had stopped working on those machines. The",
    "counter is therefore a MONOTONE-DOWN progress measure with a floor guard",
    "(> 20) rather than a completion criterion -- the opposite shape from",
    "facade_native_id_params, which was a completion criterion precisely because",
    "the type system could enforce it. There is no type-system equivalent here:",
    "both paths must compile, so nothing can make the old one unrepresentable.",
    "",
    "THE FINDING THAT RESHAPED THE PHASE, and it is a toolchain constraint rather",
    "than a design one. Every production shader travels",
    "  GLSL -> shaderc(target=vulkan 1.2) -> SPIR-V -> SPIRV-Cross -> GLSL 450",
    "       -> shaderc(target=opengl 4.5) -> OpenGL SPIR-V -> glSpecializeShader.",
    "GL_ARB_bindless_texture is a GLSL-only extension that predates SPIR-V and has",
    "no representation in the Vulkan target environment, so bindless GLSL cannot",
    "enter that pipeline at all. Pinned by BindlessShaderPipelineTest, which",
    "compiles a real bindless shader with the real target environments and fails",
    "if the answer ever changes -- including if it changes to YES, because that",
    "would make a whole compile route deletable.",
    "",
    "The consequence is the sharpest thing this rehearsal produced for Phase 4:",
    "the SHADER-SIDE mechanism does not transfer, and neither does most of the",
    "backend (residency, texture immutability, sampler folding are all",
    "ARB_bindless_texture-specific and have no VK_EXT_descriptor_heap analogue).",
    "What DOES transfer is everything above the backend interface -- views owning",
    "slots, the two lifetime classes, offsets as data, fetch-don't-store, poison",
    "on free -- which is exactly the part ADR 0011 designed and exactly the part",
    "that was worth rehearsing somewhere debuggable.",
    "",
    "ONE HAZARD THE PREVIOUS PHASE'S DESIGN CREATES, found by building on it.",
    "ResourceRegistry deliberately PRESERVES a handle across an in-place reload",
    "(ScopedResourceHandle::Sync repoints, never retires) -- that is what makes",
    "caching a ResourceHandle safe. A descriptor does not inherit that safety: an",
    "ARB_bindless_texture handle names the underlying object, so recreated storage",
    "leaves every descriptor naming a deleted object while the view's own",
    "generation is unchanged, and OffsetOf cannot detect it. This is the exact",
    "mirror image of the slice-6 finding, where stable identity made the",
    "redundant-bind cache SKIP a bind that had to happen. Same root cause,",
    "opposite symptom, same remedy: every site that recreates a resource's storage",
    "must call RHI::DescriptorHeap::InvalidateResource, exactly as it must call",
    "InvalidateTextureBinding.",
    "",
    "PHASE 3, BUCKET 3 (2026-08-03): STORAGE IMAGES. 208 -> 178.",
    "",
    "Every one of the 30 removed sites is a BindImageTexture: that family goes",
    "36 -> 6, and the 6 are the honest floor rather than remaining work. They",
    "are, exhaustively:",
    "  * RendererAPI.h (1) and RenderCommand.h (2) -- the pure-virtual",
    "    DECLARATION and the facade's declaration + forward. Nothing to convert;",
    "    the counter matches a declaration the same way it matches a call.",
    "  * HeapBindingSeam.cpp (1) -- BindImageOrOffset's OWN fallback call. This",
    "    is the degradation path and it is permanent: ARB_bindless_texture is not",
    "    universally available, so a machine without it must still bind. Deleting",
    "    this would delete the fallback, not the coupling.",
    "  * VirtualGeometryPass.cpp (2) -- deliberately NOT converted, and the",
    "    reason is worth more than the two counts. They feed",
    "    VirtualMeshGBuffer.glsl, and the bindless compile route produces no",
    "    SPIR-V and so never runs Reflect(), which leaves m_IsDeferredCapable",
    "    false and misroutes a G-Buffer shader into the forward-overlay fallback",
    "    (OpenGLShader::CreateProgramFromRawGLSL detects and errors on exactly",
    "    this). Converting the CALL SITES alone would have been safe -- the seam",
    "    forks on Shader::IsBoundProgramBindless(), so an unconverted program",
    "    still gets a real bind -- and would have bought two counter points and",
    "    nothing else. That is the shape of change this counter exists to",
    "    discourage, so they stay and say why in place.",
    "",
    "WHAT THE 30 COST, and it is NOT what 'more call sites' implies. Amendment",
    "(26) predicted a second heap-side feature before a second sweep, and that",
    "held exactly. The call-site edits were the small half:",
    "",
    "  * A SECOND DESCRIPTOR KIND. RHI::ViewUsage {Sampled, Storage} on ViewDesc,",
    "    plus StorageAccess; glGetImageHandleARB in the backend with its own",
    "    residency namespace. IDescriptorHeapBackend grew a `usage` parameter on",
    "    ReleaseDescriptor and NullDescriptor, because GL's two residency",
    "    namespaces have two entry-point pairs and the spec does not promise a",
    "    texture handle and an image handle are numerically distinguishable -- a",
    "    u64 alone cannot say which call to make.",
    "  * A SECOND RESERVED NULL SLOT (kNullStorageHeapOffset = 1). A null is per",
    "    DESCRIPTOR KIND, not per heap: `image2D(samplerHandle)` is undefined in",
    "    exactly the way `image2D(0)` is, so pointing a cleared image binding at",
    "    slot 0 would have swapped a stale-read bug for an undefined-behaviour",
    "    one. This generalises the 'unbind has no translation' finding: enumerate",
    "    the KINDS that need a null, not just the operations.",
    "  * A DISJOINT INDEX REGION in the offset table (HEAP_IMAGE_SLOT_BASE = 64,",
    "    table grown to uvec4[18]). GL image units and texture units are separate",
    "    namespaces that both start at zero, so image unit 0 and TEX_DIFFUSE",
    "    would have collided in a single-index table and each silently rendered",
    "    the other's resource.",
    "  * RESIDENCY WIDENING. glGetImageHandleARB takes no access, so a read-only",
    "    and a read-write view of the same (texture, level, layer, format) are",
    "    literally the same handle -- but reading a WRITE_ONLY-resident handle is",
    "    undefined and a second residency transition is an INVALID_OPERATION.",
    "    GTAO does exactly this within one frame (edge texture written by the",
    "    main pass, read by denoise), so the backend re-makes the handle resident",
    "    READ_WRITE and counts it.",
    "",
    "THREE DEFECTS THIS BUCKET SURFACED, none of them a type error:",
    "",
    "1. THE PERSISTENT MEMO CACHE COULD NOT TELL TWO VIEWS APART. Its key was",
    "   (resource, samplerSlot, depthCompare) -- sound only because the GL",
    "   backend declined every view that was not the whole resource, so no two",
    "   distinguishable views could reach it. HZBGenerator asks for FOUR MIPS of",
    "   one texture in one dispatch; under that key the second, third and fourth",
    "   would each have been served the first's view and the whole pyramid would",
    "   have been written at level 0 -- a plausible frame with a broken occlusion",
    "   pyramid. The key now carries the whole ViewDesc. GENERALISABLE: a cache",
    "   key that is 'sufficient' only because a downstream layer refuses the",
    "   distinguishing cases is a latent bug with a scheduled trigger date.",
    "",
    "2. InvalidateResource PUBLISHED A ZERO DESCRIPTOR on a failed re-acquire.",
    "   The comment beside it said 'poisoning renders black, which is the honest",
    "   answer'; the code stored the returned 0. A zero bindless handle is not",
    "   black -- sampling one is UNDEFINED BEHAVIOUR, which is the entire reason",
    "   NullDescriptor is a real resident texture rather than a constant. So the",
    "   failure path rested on driver luck, and it is reached precisely when",
    "   something has already gone wrong. Pre-existing; fixed here.",
    "",
    "3. Shader::SetBoundProgramBindless WAS PUBLISHED BY GRAPHICS SHADERS ONLY.",
    "   OpenGLComputeShader::Bind() never touched it, so binding a bindless",
    "   graphics program and then a compute program left the flag TRUE -- and the",
    "   first converted compute pass would have recorded offsets into a table its",
    "   program never declares while binding nothing at all. Latent rather than",
    "   live (nothing in compute was converted yet), and it would have presented",
    "   as garbage in a compute output with no diagnostic anywhere. Both Bind()s",
    "   now publish and both Unbind()s retract.",
    "",
    "ONE PLEASANT SURPRISE, recorded because it inverts the obvious estimate:",
    "COMPUTE SHADERS NEEDED NO SECOND COMPILE ROUTE. Amendment (19) established",
    "that bindless GLSL cannot enter the shaderc(vulkan) pipeline, and the",
    "graphics side needed CreateProgramFromRawGLSL to work around it.",
    "OpenGLComputeShader::Compile has ALWAYS handed include-resolved GLSL",
    "straight to glShaderSource -- it never travelled that pipeline -- so all it",
    "needed was the same #extension/#define prologue injection. Budgeting this",
    "bucket from the graphics route's cost would have over-estimated it",
    "substantially. Check whether the constraint you are routing around actually",
    "applies to the subsystem you are about to convert.",
    "",
    "AND ONE ERGONOMIC DIFFERENCE from the sampler path, which the recipe in",
    "docs/agent-rules/glsl-shaders.md now documents: an image macro DECLARES",
    "rather than expanding to an expression. The format qualifier belongs to a",
    "declaration and an image initialised from a buffer read is not a constant",
    "expression, so it cannot live at file scope -- the declaration moves into",
    "the function that uses it (WriteMip() in HZB.comp, not main()). The BODY is",
    "still byte-identical between the two variants, which is the property that",
    "keeps a conversion reviewable.",
    "",
    "PHASE 3, BUCKET 2 (2026-08-03): THE COMMAND-LAYER BIND CACHE. 178 -> 160.",
    "",
    "Renderer/Commands/ goes 21 -> 2, and BOTH survivors are the same identifier:",
    "CommandDispatch::BindTexture's definition and its declaration in the header.",
    "That is the COMMAND HANDLER'S NAME, not a bind call — the same",
    "name-collision-inflates-the-counter case the Phase 3 handover flagged for",
    "ShaderResourceRegistry::BindTexture. 2 is the floor for this directory and no",
    "amount of conversion moves it.",
    "",
    "ONLY HALF OF THIS BUCKET IS DONE, deliberately, and the other half is a",
    "DIFFERENT MECHANISM rather than more call sites (ADR 0011 amendment (32)).",
    "FlushOffsets() re-establishes the heap's SSBO binding. Per PASS that is free —",
    "the table is written once and read by every draw in the pass. The MATERIAL",
    "path changes textures per DRAW, so a converted material shader would need a",
    "flush per draw, which gives back exactly the cost bindless exists to remove",
    "('bound once per frame, never per draw'). Converting it that way would be a",
    "measurable REGRESSION that this counter would record as a win.",
    "",
    "So: every bind here is routed through the seam (correct, and inert while no",
    "bindless material program is in flight, because the seam falls back), and the",
    "material SHADERS are deliberately not converted. Skybox.glsl IS converted — it",
    "draws once per frame, so it pays the flush once and proves the",
    "command-dispatch path works end to end through the heap. The real second half",
    "is baking per-material offsets into PODMaterialData, which ADR 0011 §1.2",
    "already describes ('stable for the object's life, so it can be baked once into",
    "material data') and which is a data-layout change, not a binding one.",
    "",
    "TWO TRAPS THIS BUCKET SURFACED:",
    "",
    "1. A REDUNDANT-BIND CACHE MUST NOT SHORT-CIRCUIT AN OFFSET WRITE.",
    "   BoundTextures[slot] means 'this slot's GL BINDING is already correct',",
    "   which is sound because a binding persists until something rebinds it and",
    "   every tracked rebind goes through one helper. It does NOT mean 'this slot's",
    "   OFFSET is already correct': the offset table is shared with every pass that",
    "   binds through the seam, and those passes do not touch this array. Skipping",
    "   the write would leave another pass's offset in the slot and sample a",
    "   different real texture — and it saves nothing, because the write is a CPU",
    "   array store rather than a driver call.",
    "   HeapBinding::WritesOffsetsForBoundProgram() exists for this distinction and",
    "   for nothing else.",
    "",
    "2. THE FALLBACK HAS TO GO THROUGH THE CALLER'S RendererAPI&. The dispatch",
    "   handlers receive one by reference, and CommandDispatchTest /",
    "   FramePipelineTest execute packets against MockRendererAPI, which records",
    "   every BindTexture. Routing the fallback through the static RenderCommand",
    "   facade instead keeps compiling, keeps working in the editor, and silently",
    "   stops the mock ever seeing the call — the .data() trap of amendment (14) in",
    "   a new place: a redirection the type system cannot object to. The seam",
    "   therefore carries an api-taking overload whose ONLY job is the fallback.",
    "",
    "PHASE 3, BUCKET 1, FIRST SLICE (2026-08-03): 160 -> 146.",
    "",
    "Seven passes and nine shaders, all following the recipe #740 proved and now",
    "written up in docs/agent-rules/glsl-shaders.md section 5a: ToneMap, Bloom",
    "(threshold + downsample + upsample + composite), Vignette, Final, UIComposite,",
    "Overdraw. THE REMAINING ~85 SITES UNDER Renderer/Passes/ AND ~59 ELSEWHERE ARE",
    "UNCONVERTED — this is a slice, not the bucket.",
    "",
    "TWO RULES THIS SLICE MADE CONCRETE:",
    "",
    "1. A FLUSH PER DRAW, NOT PER PASS. Bloom rebinds slot 0 to a different mip at",
    "   every step of the downsample and upsample chains, so a flush hoisted out of",
    "   those loops would publish only the final mip's offset and every earlier draw",
    "   would sample it. Bloom has four draws and four flushes. Same rule as the",
    "   ping-pong loops in bucket 3 (FluidSmooth, GTAO_Denoise), one level up.",
    "",
    "2. DROP THE SetInt SAMPLER COMPANIONS. `m_Shader->SetInt(\"u_Texture\", 0)` was",
    "   already redundant — the shaders declare `layout(binding = N)` and",
    "   glsl-shaders.md section 5 forbids glUniform1i for samplers — but under the",
    "   bindless variant the name is a #define rather than a uniform, so each would",
    "   log a 'uniform not found' warning every frame. Converting a sampler means",
    "   deleting its SetInt, not just moving its declaration.",
    "",
    "ONE SHARED-SHADER CASE, and it is the shape section 4b predicted:",
    "FinalRenderPass and UICompositeRenderPass both draw with FullscreenBlit.glsl,",
    "so they had to convert TOGETHER. A shared shader is not a blocker; it just sets",
    "the grain of the smallest reviewable change.",
    "",
    "PHASE 3 IS NOT WORKING TODAY, AND WAS NOT BEFORE THIS BRANCH (2026-08-03).",
    "",
    "THE SUITE CAN TEST THE BINDLESS PATH. OLO_RHI_BINDLESS=1 works on the test",
    "binary exactly as it does on the editor. Nobody had run it that way — every",
    "green suite on this issue measured the SLOT path, which these conversions",
    "barely touch. Measured on this branch:",
    "",
    "    heap OFF (what CI runs)                    1 failure (vendor golden)",
    "    heap ON, #740's 11 shaders only           14 failures",
    "    heap ON, plus this branch's conversions   74 failures",
    "",
    "THE 14 ARE NOT NEW. With every shader reverted to 063b6624 the failures are",
    "FogVisualEvidence x3, VolumetricFogVisualEvidence, ContactShadowVisualEvidence,",
    "EASUVisualEvidence x3 (including the DOF one), SSAOVisualEvidence and",
    "GTAOVisualEvidence x3 — which map one-for-one onto the shaders #740 converted",
    "(SSAO, AOApply, ContactShadow, DOF, EASU, Fog). Phase 3's EXISTING rewire",
    "fails its own visual-evidence tests whenever the heap is enabled.",
    "",
    "That also reframes the 60 this branch added: 60 independent mistakes is",
    "implausible where 14 pre-existing ones share the same recipe. Expect ONE",
    "systematic defect in the conversion recipe, not sixty. Find it against the",
    "pre-existing 14 first — they are the smaller, older, already-committed",
    "reproduction, and fixing them probably fixes the rest.",
    "",
    "SO THE SHADER CONVERSIONS ON THIS BRANCH ARE REVERTED. What remains is the",
    "C++ seam work, which is safe by construction (the seam falls back for any",
    "program that is not the bindless variant), plus the mechanism, the tests and",
    "the docs. The counter therefore measures PREPARATION, not live conversion,",
    "and that distinction should stay visible until the recipe is fixed.",
    "",
    "FIRST ACTION FOR WHOEVER PICKS THIS UP: put OLO_RHI_BINDLESS=1 in CI as a",
    "second suite configuration. Until the bindless path is exercised by something",
    "automatic, any claim that it works is a claim about the slot path.",
    "",
    "PHASE 3, BUCKET 1, SECOND SLICE (2026-08-03): DDGI + PUBLISH MODE. 146 -> 119.",
    "",
    "DDGIProbeUpdatePass's 28 sites, and they split 12 PUBLISHED-GLOBAL / 16",
    "PASS-LOCAL — a distinction that needed a new seam entry point rather than",
    "more conversions, which is the third time this phase has turned out that way",
    "(storage images, per-material offsets, now this).",
    "",
    "WHY A PUBLISHED BINDING CANNOT USE THE ORDINARY SEAM. BindTextureOrOffset",
    "forks on Shader::IsBoundProgramBindless(), i.e. on the program CURRENTLY in",
    "flight. That is the right question for a pass binding its own inputs before",
    "its own draw, and a meaningless one for a slot published for LATER passes:",
    "at publish time the consumer is not bound and may not exist yet. Worse, a",
    "published slot can have consumers of BOTH kinds — TEX_DDGI_IRRADIANCE is read",
    "by the DDGI shaders and DeferredLighting (convertible) AND by Skybox_GBuffer,",
    "which can never convert because the bindless route produces no SPIR-V and so",
    "never runs Reflect(). With mixed consumers there is no correct branch.",
    "",
    "PublishTextureOffsetAndBind therefore does BOTH: stages the offset and issues",
    "the bind. A bindless consumer reads the offset, a slot-based one reads the",
    "binding, neither can tell the other happened. The cost is one redundant bind",
    "on a PER-PASS path, not per draw.",
    "",
    "CONSEQUENCE FOR THIS COUNTER, and it is not a shortfall: a publish call still",
    "contains a real bind, so it does NOT reduce the count the way a converted",
    "pass-local site does. The ratchet will always move less than the raw site",
    "count suggests once published state is involved. Do not chase the difference.",
    "",
    "ONE CONVERSION FAILED AND FELL BACK SILENTLY, caught by a test that looks",
    "like housekeeping. DDGI_Relight.glsl declares u_ShadowMapCSMRaw /",
    "u_ShadowAtlasRaw itself AND includes DeferredLightingShared.glsl, which",
    "declares them too. A `#define u_X ...` rewrites EVERY occurrence of the token",
    "in the translation unit, so the header's declaration became garbage, the",
    "bindless build failed, and the engine degraded to the slot path with nothing",
    "but an error log to show for it. The only durable trace was the",
    "`.bindless.failed.glsl` dump — which",
    "AssetContentValidity.AllCacheFilesMatchKnownPattern flagged as an",
    "unclassified cache file. THAT is what turned a silent degradation into a red",
    "test. Reverted that one shader to the slot path (the other four DDGI shaders",
    "are converted); the rule is now in glsl-shaders.md section 5a: grep the",
    "include tree for the name before converting a declaration.",
    "",
    "PHASE 3, BUCKET 1, RESUMED (2026-08-04): 87 -> 53, AND THE SHADERS ARE BACK.",
    "",
    "THE RECIPE WAS NEVER THE PROBLEM. The revert above concluded 'expect ONE",
    "systematic defect in the conversion recipe, not sixty' and was right about",
    "the count and wrong about the location: #747's final commits fixed the",
    "defects (a unit test that shut down the process-wide heap and never restored",
    "it, an un-rebased offset table across a heap epoch, a static-destructor",
    "teardown at atexit, a leak-detector constant that had drifted), taking heap-ON",
    "from 13 failures to 1. All 30 reverted .glsl/.comp files were restored here by",
    "reverse-applying the revert, in six batches, and EVERY batch held parity:",
    "",
    "    heap OFF   5417 passed, 1 failed   (AtmosphereVisualEvidence, #735)",
    "    heap ON    5417 passed, 1 failed   (the same one, same RMSE)",
    "",
    "So the conversions the revert backed out were fine. What was broken was the",
    "environment they ran in. A wholesale revert of N changes to find 1 shared",
    "cause costs the N changes; bisecting to the shared FIXTURE would have cost",
    "less. The revert's own bisect data even pointed at it — 'reverting ToneMap +",
    "FullscreenBlit alone took 74 -> 40' describes a per-frame ORDERING effect,",
    "not two bad shaders, and those two now pass untouched.",
    "",
    "VERIFY THAT A CONVERSION RAN, NOT THAT THE SUITE IS GREEN. A failed bindless",
    "build degrades to the slot path silently, so green is satisfied just as well",
    "by a conversion that never executed. The check is the engine log's list of",
    "shaders that took the route (50 today, up from 11) plus a zero count of",
    "'.bindless.failed.glsl' dumps and fallback warnings. It has THREE message",
    "shapes and missing any one reads as a silent fallback: a fresh graphics build",
    "logs \"'<Name>' built through the raw-GLSL route\"; a later run that hits the",
    ".pgr cache logs 'Loaded bindless program from binary cache: <path>'; and",
    "COMPUTE logs neither — only a '(bindless)' suffix on its ordinary compile",
    "line, because OpenGLComputeShader has always fed raw GLSL to glShaderSource",
    "and never needed the raw-GLSL route at all.",
    "",
    "THE ASYMMETRY THAT MAKES A PARTIAL CONVERSION DANGEROUS, and it is new as of",
    "this batch. Once a shader has ANY converted input it builds as the bindless",
    "variant, so Shader::IsBoundProgramBindless() is true for the whole program.",
    "Converting a C++ bind for an input whose declaration is still",
    "`layout(binding = N)` then records an offset and issues NO bind, leaving that",
    "sampler unbound and reading black. FroxelFogScatter.comp was exactly this: its",
    "output image was converted, its three shadow/history samplers were not.",
    "Section 5b's 'a shader can be moved input by input' still holds, but the unit",
    "of movement is the C++ bind AND its declaration TOGETHER. This hazard only",
    "exists once a file is partly converted — i.e. it did not exist before this",
    "session and now applies to 30 files.",
    "",
    "A HEAP HANDLE CARRIES NO TYPE. JumpFlood_Init samples an R32I entity-ID target",
    "through an isampler2D, and BindlessHeap.glsl had float/array/3D/cube sampler",
    "macros only. sampler2D(h) and isampler2D(h) are different reinterpretations of",
    "the SAME uvec2, so the float macro would have read garbage rather than failed",
    "to compile. Added OLO_HEAP_ISAMPLER_2D / OLO_HEAP_TEX_2D_INT — and only the",
    "integer form actually exercised, because an unused macro in that header is",
    "dead code that can be wrong unnoticed, which is precisely how",
    "g_OloHeapOffsets[16] survived while the engine wrote 18.",
    "",
    "THE COUNTER IS AT ITS FLOOR: 22, and every remaining site is justified.",
    "",
    "    HeapBindingSeam.cpp        4  the seam's OWN slot-path fallbacks, which are",
    "                                  permanent — a device without ARB_bindless_texture",
    "                                  has nothing else to take.",
    "    RenderCommand.h            4  facade declarations.",
    "    ShaderResourceRegistry     4  a NAME COLLISION, not bind sites: a differently",
    "                                  shaped member taking a ResourceBinding.",
    "    VirtualGeometryPass.cpp    2  deliberate deferrals (G-Buffer producer). Was 3",
    "                                  until the HZB occlusion input was converted.",
    "    RGCommandContext           3  the wrapper that forwards into the seam.",
    "    RendererAPI.h              2  facade declarations.",
    "    CommandDispatch            2  the handler's own name.",
    "    FogRenderPass.cpp          1  TEX_SHADOW — NEW, and found by applying the §5c",
    "                                  rule written this session. PostProcess_Fog.glsl",
    "                                  is a bindless VARIANT, but the shadow array is",
    "                                  declared in the shared DeferredLightingShared.glsl,",
    "                                  so a seam call would record an offset, skip the",
    "                                  bind and leave the sampler dark. Convert only when",
    "                                  TEX_SHADOW moves in the shared header, and then",
    "                                  across every consumer at once.",
    "",
    "The Fog site is why the floor did not fall further; converting the HZB",
    "occlusion input in VirtualGeometryPass is why it reached 22. The itemised",
    "counts above sum to exactly that — if they ever stop summing to the recorded",
    "sweep_bind_texture_sites, one of the two has drifted and the list is the half",
    "nobody re-runs.",
    "",
    "ONE SHADER IS DELIBERATELY UNCONVERTED, and it is a new class of blocker.",
    "Particle_Billboard_GPU.glsl reads gl_InstanceIndex — the VULKAN spelling. The",
    "default path is shaderc(vulkan) -> SPIR-V -> SPIRV-Cross -> GLSL, and",
    "SPIRV-Cross is what rewrites it to gl_InstanceID. The bindless route bypasses",
    "that translation, so the builtin reaches the GL compiler verbatim:",
    "'error C7531: global variable gl_InstanceIndex requires",
    "\"#extension GL_KHR_vulkan_glsl : enable\" before use'. Renaming is NOT a fix —",
    "the default path targets Vulkan, where gl_InstanceID does not exist. Any shader",
    "naming a GL_KHR_vulkan_glsl builtin is unconvertible until the bindless route",
    "gains a translation step (glsl-shaders.md §5c-bis).",
    "",
    "IT FAILED SILENTLY WITH THE SUITE GREEN. 5419 passed / 1 failed in BOTH",
    "configurations while that shader was quietly not converted; the only trace was",
    "the .bindless.failed.glsl dump. Counting those dumps per run is what caught it,",
    "and is the reason that count belongs in any verification harness here.",
    "",
    "THE COUNTER BEING AT ITS FLOOR DOES NOT MEAN PHASE 3 IS DONE, and conflating",
    "the two is the mistake this file exists to prevent. The counter measures C++",
    "BIND SITES. Phase 3's text is 'rewire the ~125 texture-bind sites, UPDATE",
    "GLSL', and the GLSL half is not finished:",
    "",
    "    152 production shaders, 62 carry OLO_BINDLESS.",
    "    ~48 sample or store textures and are not converted.",
    "",
    "TWO OF THE THREE 'BLOCKERS' WERE SOFT AND ARE NOW FIXED IN THE ENGINE. Both",
    "had been written up as structural limits; neither was.",
    "",
    "  * VULKAN-ONLY BUILTINS. The bindless route bypasses SPIRV-Cross, which is",
    "    what translates gl_InstanceIndex / gl_VertexIndex into their GL spellings.",
    "    CreateProgramFromRawGLSL now performs that substitution itself, using",
    "    SPIRV-Cross's OWN output for these shaders as the ground truth",
    "    (gl_InstanceIndex -> gl_InstanceID, gl_VertexIndex -> gl_VertexID) rather",
    "    than a reading of the spec. Deliberately gl_InstanceID ALONE: SPIRV-Cross",
    "    prints gl_InstanceID + SPIRV_Cross_BaseInstance, but guards that term on",
    "    GL_ARB_shader_draw_parameters, which this engine does not enable — so every",
    "    existing draw indexes with + 0, and adding the real base instance made the",
    "    raw route disagree with the slot path about which instance a vertex belongs",
    "    to. Match what the other path COMPILES TO, not what the translator prints.",
    "    Particle_Billboard_GPU converted as a result. gl_BaseInstance and",
    "    gl_BaseVertex are NOT bridged and remain blockers.",
    "",
    "  * G-BUFFER PRODUCERS. The route already scanned the source for exactly this",
    "    condition — only to ERROR. It now derives m_IsDeferredCapable from that",
    "    scan, mirroring Reflect()'s criteria and matching a DECLARATION rather than",
    "    a mention (DeferredLighting samples gAlbedo and must not be misread as a",
    "    producer). The 'second missing piece' #747 cited, the resource registry's",
    "    discovered bindings, turned out to have ZERO consumers: SetResource is",
    "    called nowhere in the engine or the editor.",
    "",
    "AND ONE CLASSIFICATION WAS SIMPLY WRONG. VirtualClusterCull.comp was filed as",
    "Vulkan-blocked on a grep that matched a COMMENT ('// gl_BaseInstance keys the",
    "VisibleCluster record'); Foliage_Impostor.glsl the same. Re-run on",
    "comment-blanked text — the same discipline RHIBoundaryRatchetTest applies —",
    "the counts are 4 Vulkan-blocked and 12 G-Buffer, not 5 and 13. That is why",
    "VirtualGeometryPass's HZB site converted and the counter reached 22: it was",
    "never a floor entry, and calling it a 'deferral' was right by accident.",
    "",
    "THE MATERIAL PATH IS BUILT AND PROVEN, AND BLOCKED ON ONE MISSING INPUT.",
    "PODMaterialData/PBRMaterialUBO now carry nine per-material heap offsets,",
    "resolved through HeapBinding::ResolveTextureOffset, cached on (material index,",
    "heap epoch, program kind), with BindPBRTextures skipping all nine",
    "glBindTextureUnit calls when the program reads them. PBR_MultiLight compiled",
    "and ran through it.",
    "",
    "TWO SUPERSEDED DIAGNOSES OF WHY IT IS REVERTED, both recorded because the way",
    "they failed is the useful part. (1) 'A BINDLESS DESCRIPTOR BAKES SAMPLER STATE':",
    "SamplerDesc{} defaults to ClampToEdge + no anisotropy, right for a render target",
    "and wrong for a material texture. Retracted — supplying the real sampler state",
    "made the measurement WORSE (5.861 -> 10.447). (2) 'THE RAW-GLSL COMPILE PATH",
    "COSTS PRECISION': a no-op OLO_BINDLESS block, changing the compile ROUTE and",
    "nothing else, also measured 5.861, so the cost was attributed to the route's",
    "float codegen and called a pre-existing floor from #740. Also wrong.",
    "",
    "THE ACTUAL CAUSE IS §5c, AND THE SCALAR IS WHAT HID IT. WorldOriginRebase",
    "compares two frames in the SAME configuration, so a defect that darkens BOTH",
    "equally is invisible to it; 1.413 -> 5.861 was only the residual noise scaling",
    "with contrast. Comparing ACROSS configurations instead: RMSE 78.8 on the far",
    "frame and 78.6 on the near one (62% of pixels; mean luminance 128 -> 68). Equal",
    "on both frames means it was never precision at all.",
    "",
    "Bisected by probing the fragment shader and reading the pixels: `ambient`",
    "identical (RMSE 0.000), the pre-shadow per-light contribution identical (0.002),",
    "`Lo` collapsed (105.2). Only the shadow samplers differed — and they are exactly",
    "the inputs PBR_MultiLight still declares as `layout(binding = 8/33)` while its",
    "bind (BindTrackedTextureUnit -> HeapBinding::BindTextureOrOffset) forks on",
    "Shader::IsBoundProgramBindless(). Taking the route made that true, so the seam",
    "staged an offset and issued NO bind, the shadow lookups read black, and direct",
    "lighting died while ambient survived. Textbook §5c, from the branch's own docs.",
    "",
    "IT WAS ALREADY SHIPPED SOMEWHERE ELSE. Terrain_Depth.glsl converted",
    "u_TerrainHeightmap with a proper #ifdef/#else pair and left u_SnowDepthMap,",
    "declared on the NEXT LINE, slot-based — so snow deformation silently read zero",
    "under OLO_RHI_BINDLESS=1, with the whole suite green in both configurations.",
    "Now fixed, and the prose rule is now MACHINE-CHECKED by",
    "BindlessShaderPipeline.NoBindlessRouteShaderKeepsASlotBasedSamplerDeclaration",
    "(verified to fail on the real bug and pass once fixed, not merely to pass).",
    "The one legitimate exception it allows is a slot published through",
    "PublishTextureOffsetAndBind, which stages an offset AND always binds.",
    "",
    "THE MATERIAL CONVERSION WAS THEN ATTEMPTED IN FULL AND IS REVERTED AGAIN, with",
    "a much sharper failure than last time. All seven contract shaders were converted",
    "whole. On the rebase scene the result was PIXEL-IDENTICAL to the slot path —",
    "cross-config RMSE 0.0013, 0.000% of pixels differing, mean luminance 127.939 vs",
    "127.939, and the shader verifiably on the raw route (spirvMap stages=0, a",
    ".bindless.pgr written, 0 fallbacks). That measurement was real and it was also",
    "OVER-GENERALISED: it is ONE scene. The wider visual set failed three DDGI tests.",
    "",
    "Bisected cleanly — C++ alone passes DDGI 5/5, so it is the GLSL; then, within the",
    "shaders: DepthPrepass_Mask* are SAFE (kept, and shipped here), PBR_GBuffer* and",
    "VirtualMeshGBuffer break LightLeakRegression + MultiAngleGoldens, and",
    "PBR_MultiLight* break ForwardPlusPathSmokeAsymmetry. Reverting ONLY the four",
    "shadow-sampler declarations in PBR_MultiLight makes it pass: the dark band goes",
    "40.7 -> 33.0 against a lit band of ~60, i.e. converting the shadow samplers makes",
    "shadows LEAK rather than fail outright.",
    "",
    "THE CAUSE IS NOT FOUND, and three plausible ones are now excluded so the next",
    "attempt does not re-spend them: (a) the env/IBL offset SOURCE — switching those",
    "four from the shared table to the per-material lanes moved the bands by 0.02;",
    "(b) MIP FILTERING — SamplerDesc defaults LinearMipFilter = true while the shadow",
    "arrays have one level, which is genuinely wrong and is fixed here, but fixing it",
    "produced numbers identical to the last digit (the signature of nothing changing);",
    "(c) a FAILED DESCRIPTOR falling back to the reserved null, which would explain an",
    "unshadowed read via §5d type confusion — the log shows zero descriptor failures.",
    "",
    "WHY IT IS NOT SHIPPED HALF-CONVERTED. PBR_MultiLight with its shadow declarations",
    "left slot-based passes the tests, and that is exactly the §5c violation this",
    "phase's own guard forbids: it works only because some other pass happens to bind",
    "those slots, which is a coincidence with a scheduled expiry date. The guard fails",
    "it, correctly, and the guard was believed.",
    "",
    "ONE PIECE OF GROUNDWORK IS DELIBERATELY INERT: the shadow SamplerDesc",
    "(ClampToBorder + opaque-white border + LEQUAL + no mip filtering, all read off",
    "OpenGLTexture2DArray) and RHI::BorderColor. No converted shader reads a shadow",
    "map through the heap today, so none of it is exercised — it is correct by",
    "construction and UNVERIFIED by test, and the next attempt needs it before it can",
    "get anywhere. Said plainly rather than counted as done.",
    "",
    "THAT QUESTION IS NOW ANSWERED AND FIXED (commit 'a published descriptor is",
    "engine-global'). A slot bind samples with the TEXTURE's state, so every shadow",
    "consumer got GL_COMPARE_REF_TO_TEXTURE for free; a heap descriptor samples with",
    "the sampler OBJECT baked into its handle, and the seam's default SamplerDesc{}",
    "carries Compare = Never. Four sites stage a shadow offset and ONE of them",
    "PUBLISHES (DDGIProbeUpdatePass), so a single site defaulting its sampler",
    "disabled comparison for every bindless reader that frame — a last-writer race.",
    "The state now lives in HeapBinding::ShadowDepthSampler and all four call it.",
    "VolumetricFogPass was the same defect in SHIPPED code: FroxelFogScatter.comp",
    "reads as sampler2DArrayShadow while the pass bound with the default sampler, so",
    "volumetric fog's shadow lookups were undefined with the suite green.",
    "",
    "WITH THAT FIXED, THE MATERIAL CONVERSION GOES FROM 3 DDGI FAILURES TO 5/5",
    "PASSING — Forward+ bands 60.40/32.89 against a slot-path baseline of",
    "60.41/33.03 — and every DDGI, CameraRelative and SceneRender test passes with",
    "all seven contract shaders converted. It is STILL not shipped, and the residue",
    "is now one specific, reproducible thing rather than a fog:",
    "",
    "  WorldOriginRebase's MultipleSequentialRebases reports 'visible pop at rebase",
    "  boundary 2' (RMSE 8.10) and boundary 3 (7.64) — but ONLY when a sibling test",
    "  has already run in the same process. Alone it passes warm, warm-again, and",
    "  cold; its traversal frames are then cross-config IDENTICAL (RMSE 0.001-0.002,",
    "  consecutive-step RMSEs matching to three decimals in both configurations).",
    "  So the RENDERING is right and something process-scoped decays across scenes.",
    "",
    "FOUR MORE HYPOTHESES EXCLUDED BY CONTROLLED RUNS, not by argument, so the next",
    "attempt spends its time elsewhere: the env/IBL offset SOURCE (shared table vs",
    "per-material lanes — both fail identically); the SHADOW slots (reverting just",
    "those four declarations changes nothing); DDGI as the contaminator (DDGI +",
    "rebase alone passes at 1.413 — the trigger is a sibling rebase test); and the",
    "MATERIAL UBO CACHE keyed on material index (disabling it while offsets are live",
    "changes nothing, so the index is evidently not aliasing distinct descriptors).",
    "",
    "FOUND, and it is none of the above: THE RESERVED NULL HAS A TYPE, AND A",
    "samplerCube BUILT FROM IT IS UNDEFINED BEHAVIOUR.",
    "",
    "kNullHeapOffset names a 1x1 2D RGBA8 texture. The rebase scene's materials carry",
    "NO textures — every handle is invalid (ResourceHandle index 0xFFFFFFFF) — so",
    "every material and IBL lane resolves to that null. The converted shader then",
    "does `samplerCube(g_OloResourceHeap[0])` for the environment probe and the IBL",
    "trio. ARB_bindless_texture: using a handle with a sampler type that does not",
    "match the texture's target is UNDEFINED. Undefined is exactly what varies with",
    "whatever ran before it, which IS the order dependence — no state was leaking at",
    "all.",
    "",
    "PROVED BY REMOVING ONLY THAT: keep the material five converted, keep the four",
    "shadow samplers converted, and take just the three samplerCube reads plus the",
    "BRDF LUT off the heap — both rebase tests PASS. Nothing else changed.",
    "",
    "THE ENGINE ALREADY KNEW THE RULE AND APPLIED IT ONE LEVEL TOO SHALLOW. This file",
    "says of the storage-image null: 'A null is per DESCRIPTOR KIND, not per heap:",
    "image2D(samplerHandle) is undefined in exactly the way image2D(0) is.' That is",
    "the same argument, and it does not stop at sampler-vs-image — samplerCube,",
    "sampler2DArray and sampler2DArrayShadow each need their own null, and nothing",
    "enforces it. §5d ('heap handles carry no type') states the hazard; the null",
    "descriptor is the one place the engine MANUFACTURES a type mismatch by itself.",
    "",
    "THE FIX IS SMALL AND FULLY SPECIFIED. The infrastructure is already shaped for",
    "it: kNullHeapOffset = 0, kNullStorageHeapOffset = 1, kFirstAllocatableHeapSlot",
    "= 2. Add kNullCubeHeapOffset = 2 (and, for completeness, array + array-shadow",
    "nulls), create the 1x1 cubemap beside m_NullTexture in",
    "OpenGLDescriptorHeapBackend::Initialize with the same fail-closed handling, give",
    "IDescriptorHeapBackend::NullDescriptor a KIND alongside its ViewUsage, and have",
    "the resolve sites pass the kind they already know — WriteMaterialHeapOffsets",
    "distinguishes k2DSampler from kCubeSampler today, and for the shared table the",
    "TEX_* slot implies the kind. ResetScratchToNulls then seeds each region with its",
    "own null exactly as it already does for the image region.",
    "",
    "LOCALISED, AND THE VERTEX-PATH DIAGNOSIS BELOW WAS WRONG. The typed null did",
    "NOT fix the pop, and neither did the vertex path: pinning the instance fetch to",
    "instances[0] changes nothing, and every draw in this scene is single-instance",
    "anyway, so gl_InstanceIndex was already 0 and that experiment was vacuous. The",
    "snowWeight lead that produced it was read through the FULLY POST-PROCESSED",
    "UIComposite, which cannot separate the forward pass from anything downstream.",
    "",
    "THE SYMPTOM IS NOT SHADING AT ALL — IT IS DROPPED GEOMETRY. Make the material",
    "emit a CONSTANT colour and the sphere still changes across a rebase, which a",
    "constant-shaded object at a fixed screen position cannot do. Looking at the PNG",
    "instead of its statistics: the sphere is punched through with blotchy holes,",
    "silhouette intact, and the holes show SKY — not the ground BEHIND the sphere.",
    "Something wrote depth there without colour. Direct count: 23340 hole pixels",
    "under heap-on against 1227 (the sky in the bbox) under heap-off.",
    "",
    "CAUSE: THE COMPILE ROUTE IS OBSERVABLE THROUGH THE DEPTH BUFFER. The depth",
    "prepass declares invariant gl_Position and the colour pass re-tests LEQUAL",
    "against the depth it wrote. invariant constrains ONE compiler; it cannot make",
    "shaderc->SPIR-V->SPIRV-Cross and the driver GLSL front-end round the same",
    "expression identically. DepthPrepass.glsl declares NO SAMPLERS, so it never",
    "mentions OLO_BINDLESS and a sampler-driven sweep walks straight past it:",
    "converting the material bucket moved PBR_MultiLight to the raw route and left",
    "its own prepass on the SPIR-V one. This is a rule §5c cannot express, because",
    "§5c is about declarations and binds and this file has neither.",
    "",
    "PROVED BOTH WAYS. Make the prepass write no depth: holes 23340 -> 1227, the",
    "heap-off baseline exactly. Put the prepass back on the same route via",
    "OLO_BINDLESS_ROUTE_PARITY: holes 23340 -> 1227 WITH the prepass still writing",
    "depth, and both configurations then score an IDENTICAL 1.329. Now guarded by",
    "BindlessShaderPipeline.DepthInvariantShadersAgreeOnTheCompileRoute, verified to",
    "fail when the group is split and to name the offending file.",
    "",
    "WHY IT SURVIVED SO LONG, worth recording because the shape recurs: 23340",
    "dropout pixels on the sphere and ZERO on the ground plane in the same frame. A",
    "curved surface's steep per-pixel depth gradient turns a last-bit disagreement",
    "into a visible hole; a plane absorbs it. So it reads as 'that mesh is broken'",
    "and every hypothesis went to shading. All 24 PostProcess_*, SSAO, SSAO_Blur,",
    "SSS_Blur, the HZB/cluster/occlusion culling shaders, shadows, velocity/TAA and",
    "material colour were each excluded by a controlled run before the prepass was",
    "suspected at all.",
    "",
    "ONE HARNESS TRAP COST A WHOLE ROUND AND IS THE REUSABLE LESSON. The reproducer",
    "is WorldOriginRebaseVisualEvidenceTest.* — BOTH tests, ~2 min. Narrowing the",
    "filter to the FAILING test alone REMOVES the phenomenon (it needs the sibling to",
    "have run first in-process), so four consecutive probes reported 'no difference'",
    "because there was none to find. A probe harness must re-measure the UNPROBED",
    "control every run; probe_run.ps1 now does, and prints it.",
    "",
    "FOUR HYPOTHESES DIED ON THE WAY, each by a controlled run, and the instrument",
    "that killed the last one is worth keeping in mind: dumping the shared table's",
    "STAGED SET (not its values) showed the passing and failing runs byte-identical,",
    "which retired the stale-slot theory that the ResetScratchToNulls comment above",
    "had made so attractive. A matching signature is a lead, not a diagnosis — and",
    "the same dump showed the material lanes were never in that table at all, which",
    "is why swapping env/IBL between the shared table and the per-material lanes had",
    "changed nothing: both resolve to the same wrongly-typed null.",
    "",
    "SO: the C++ counter is at 22 and every remaining site is justified, and the GLSL",
    "half is NOW FINISHED — the material bucket (PBR_MultiLight{,_Skinned},",
    "PBR_GBuffer{,_Skinned}, VirtualMeshGBuffer) is converted and shipped, with",
    "DepthPrepass{,_Skinned} carried onto the same route by OLO_BINDLESS_ROUTE_PARITY",
    "so the depth-invariance group cannot split. Both configurations green.",
    "",
    "THE SAMPLER HALF OF THE NULL-TYPING PROBLEM IS NOW FIXED. A view records its",
    "NullSamplerKind on the SLOT — deliberately not in ViewDesc, whose defaulted",
    "operator== is the memoisation key, so a kind field there would split one",
    "texture into two descriptors the moment two call sites disagreed. Both paths",
    "that write a null now use it: ReleaseSlotLocked captures the kind before the",
    "slot is reset (as it already did for usage), and the failed-reacquisition",
    "path reads slot.NullKind. Without this, a cached material UBO still holding a",
    "retired cube view's offset sampled a 2D descriptor through samplerCube —",
    "undefined, and the same defect the typed nulls were added to remove, one",
    "level down from where they were added. Pinned by PoisonKeepsTheViewsSamplerKind",
    "and FailedReacquisitionKeepsTheViewsSamplerKind.",
    "",
    "THE STORAGE HALF IS NOW FIXED TOO, and the record of how it was scoped is",
    "worth keeping because the scoping was wrong three times. It was first deferred",
    "as 'a design decision'; then narrowed to 'only the cleared-binding case needs",
    "reserved slots'; then a memoised per-format null VIEW was floated as a way to",
    "avoid reserved slots entirely. That last idea is impossible: the memo cache is",
    "keyed on ResourceHandle and the backend null image has no engine-side handle to",
    "key on. Reserved slots per format was the right design from the start.",
    "",
    "WHAT IT LOOKS LIKE. Five more reserved slots (RGBA32F, R8, RGBA16F, RGBA8,",
    "R32UI) beside the existing R32Float one, which keeps offset 1 so every test",
    "naming it keeps its meaning; kFirstAllocatableHeapSlot moves 5 -> 10. A",
    "NullStorageDescriptor(Format) virtual sits BESIDE NullDescriptor rather than",
    "widening it, because an image handle is typed by FORMAT and a sampler handle by",
    "TARGET — two axes needing the same treatment, not one axis with more cases. The",
    "GL backend creates and retires one 1x1 null image per format; poison, failed",
    "reacquisition and cleared bindings all carry the view's own FormatOverride.",
    "",
    "ONE TRAP WORTH REPEATING: glTextureStorage2D leaves contents UNDEFINED, not",
    "zeroed — the same reason the heap buffer is prefilled rather than trusted — so",
    "each null image is cleared explicitly.",
    "",
    "Pinned by PoisonKeepsTheViewsStorageFormat (all five formats, distinct backend",
    "sentinels per format so a wrong pick is visible rather than merely plausible),",
    "EveryReservedNullSlotIsPublishedOnTheFirstFlush (all ten slots) and the",
    "BindlessShaderPipeline distinctness guard, widened from 5 offsets to 10.",
    "",
    "ONE PASS IS CONVERTED BUT UNVERIFIABLE BY THE SUITE: Terrain_Erosion.comp is",
    "created only by the editor's terrain brush and NO test instantiates it, so its",
    "bindless variant is exercised in the editor or not at all. Confirmed live:",
    "the editor under OLO_RHI_BINDLESS=1 logs 46 distinct shaders on the bindless",
    "route including \"Compiled compute shader 'Terrain_Erosion' (bindless)\", with",
    "0 shader errors, 0 'Not a valid texture' and 0 fallbacks.",
    "",
    "THE LIVE ON-VS-OFF A/B IS NOT AN INSTRUMENT HERE, and this retires a standing",
    "'open work' item rather than adding one. Four fixed orbit poses on the water",
    "sandbox, settleFrames=12: the ON-vs-OFF diffs are 8.4%-31.6% (RMSE 4.68-9.19)",
    "against a SAME-CONFIG, SAME-POSE noise floor of 23.5% (RMSE 9.74). Two captures",
    "of one build differ by more than the two builds differ from each other, because",
    "the scene animates water, three vehicles and a TAA history. #740 read",
    "'signal ~= floor' as parity and was wrong to; reading it as a regression would",
    "be equally wrong. The measurement resolves nothing in either direction.",
    "",
    "The deterministic A/B already existed: the suite's 80 golden and",
    "visual-evidence tests have fixed cameras and committed goldens and run under",
    "OLO_RHI_BINDLESS=1 unchanged. All 80 ran (0 skipped) and passed in BOTH",
    "configurations with OLOENGINE_GOLDEN_REBASE unset. That is the pixel evidence",
    "for this phase; the live capture is for 'it renders and the log is clean'.",
    "BUCKET 1 IS CLOSED (2026-08-07), AND \"CLOSED\" NOW MEANS SOMETHING A TEST CAN",
    "CHECK. 53 shaders declared a slot-based sampler when this slice started; 17 do",
    "now, and every one of the 17 carries a recorded reason rather than a silence.",
    "36 were converted. The route list grew 71 -> 107 (87 graphics + 20 compute), with",
    "0 .bindless.failed.glsl dumps, 0 fallback warnings and 0 'Not a valid texture'.",
    "Both configurations: 5427 passed / 1 failed of 5432, the failure being the pre-existing",
    "AtmosphereVisualEvidenceTest (#754), which also fails with the heap off.",
    "",
    "WHY IT HAD NOT CLOSED BEFORE, and this is the reusable part: an unconverted",
    "shader RENDERS CORRECTLY. The seam forks per program, so a slot-based one takes",
    "the fallback and gets a real bind. That makes \"left slot-based on purpose\" and",
    "\"nobody got to it\" indistinguishable from outside, and it is why '39 remaining'",
    "survived three review rounds without the number ever meaning anything. The fix is",
    "not more converting, it is an end condition:",
    "BindlessShaderPipeline.EveryShaderIsOnTheRouteOrExplicitlyExcluded fails on any",
    "sampler- or image-declaring shader that is neither converted nor listed with a",
    "reason, and checks the list in BOTH directions so it cannot rot into a silencer.",
    "Verified to fail by reverting one converted shader (it named the file) and, on",
    "its very first run, by catching a hand-written std::array size that was one too",
    "large — the phantom entry reported ' is listed but does not exist'. It is",
    "std::to_array now.",
    "",
    "THE BLOCKER WAS FOUR LINES, AND IT WAS NOT THE ONE ON RECORD. ADR 0011 amendment",
    "(32) declined the per-draw paths because 'a converted shader needs a flush per",
    "draw, which gives back exactly the cost bindless exists to remove'. Written about",
    "MATERIALS, where per-material offsets in the material UBO were and remain the",
    "right answer — then reused as a general reason for terrain, foliage, decals and",
    "water, and never re-derived. The cost it names is the TABLE UPLOAD, and",
    "HeapBinding::StageOffset was marking the table dirty even when writing the value",
    "already there. Consecutive draws in a bucket overwhelmingly restage the SAME",
    "offsets (the same terrain textures across every patch), so one `if` makes those",
    "flushes a bool test. The redundant-bind cache cannot suppress the writes —",
    "amendment (32)'s own second finding is that it must not — so the stage is the",
    "only place the distinction can be made. What genuinely remains per draw is",
    "DescriptorHeap::Flush's unconditional heap rebind: one glBindBufferBase against",
    "the up-to-nine glBindTextureUnit calls the conversion deletes.",
    "",
    "GENERALISABLE: WHEN A COST ARGUMENT STARTS BLOCKING MORE THAN IT WAS MEASURED ON,",
    "RE-DERIVE IT. This one cost roughly two thirds of the shader tree.",
    "",
    "A TEXTURE BIND HAS TWO SPELLINGS AND sweep_bind_texture_sites COUNTS ONE. The",
    "counter's needle is `BindTexture(` / `BindImageTexture(` — the RendererAPI",
    "facade. `Texture::Bind(slot)` is the same act through the resource object, and",
    "25 such sites existed. SEVENTEEN OF THEM WERE THE REASON SEVEN SHADERS LOOKED",
    "UNCONVERTIBLE: IBLPrecompute, AssetPreviewRenderer, ImpostorBaker and",
    "FoliageRenderer bound their inputs that way, the seam never saw them, and a",
    "converted shader would have read an offset nobody staged — black, with no",
    "diagnostic. All seventeen now route through the seam.",
    "",
    "The eight that remain are deliberate and three are LOAD-BEARING: ShadowMap (x2),",
    "WindSystem and SnowAccumulationSystem are exactly the 'a direct Texture::Bind()",
    "that never consults the seam' mechanism the §5c allowlist depends on, because a",
    "shared include/ header converts all-or-nothing. The rest are",
    "ShaderResourceRegistry (x2), Renderer2D's excluded quad batcher, and VideoTexture.",
    "",
    "NO COUNTER WAS ADDED FOR THE SECOND SPELLING, and that is the finding rather than",
    "a gap. `Bind` is an overloaded name across SoundGraph, Templates/Function.h and",
    "every buffer type, so no text rule tight enough to exclude those also catches",
    "`m_CSMTextureArray->Bind(slot)`. A ratchet whose rule is 'the identifiers I could",
    "think of' is precisely the false confidence §1.3's survey exists to prevent. The",
    "completeness test measures the property instead of a proxy for it.",
    "",
    "TWO ORDERINGS DECIDE WHETHER A CONVERSION WORKS AT ALL, and neither is visible in",
    "the shader. (a) The seam forks on the program IN FLIGHT, so a bind issued before",
    "shader->Bind() always takes the fallback — IBLPrecompute binds its environment",
    "cube first every time, which is why those five sites became",
    "PublishTextureOffsetAndBind rather than the forking form. (b) A staged offset is",
    "CPU-side until the flush. Every draw in CommandDispatch now publishes",
    "unconditionally, because WHICH handler owes a flush is a property of the shader",
    "it dispatches and that changes as shaders convert — pairing them per site is a",
    "rule that must be re-derived on every .glsl edit, and it fails the quiet way.",
    "",
    "THE FOUR EXCLUSION REASONS ARE GENUINELY DIFFERENT, which is why one 'not yet'",
    "bucket would have been worse than none:",
    "  1. A SHARED include/ HEADER converts all-or-nothing — its own #ifdef",
    "     OLO_BINDLESS IS the route opt-in token, so a converted declaration drags",
    "     every includer, present and future, onto the raw-GLSL route whether or not",
    "     that shader wanted to give up SPIR-V reflection. Worth knowing before anyone",
    "     'fixes' this: four of the five now have an all-converted includer set, so it",
    "     WOULD work today. The reason not to is that it makes the compile route a",
    "     property of a header, after which #include becomes the one edit that",
    "     silently changes where a shader compiles. WindSampling.glsl still has a",
    "     slot-based includer (compute/Particle_Simulate.comp) either way.",
    "  2. A SAMPLER TARGET WITH NO RESERVED NULL — DeferredLighting_MSAA's",
    "     sampler2DMS. There is no 1x1 multisample texture in the reserved set and no",
    "     OLO_HEAP_TEX_* form to reach one, so converting it would hand-reintroduce",
    "     the wrongly-typed-null defect that cost four wrong diagnoses on the rebase",
    "     pop. Adding the target is bounded work; doing it for one shader on the MSAA",
    "     deferred path is not what closes a bucket.",
    "  3. BINDLESS REPLACES THE MECHANISM, NOT THE DECLARATION — Renderer2D_Quad's",
    "     `sampler2D u_Textures[32]` plus its 32-case switch is a workaround for",
    "     exactly the limit bindless removes; the real conversion carries a per-quad",
    "     heap offset in the VERTEX FORMAT. Its sibling Renderer2D_Text IS converted:",
    "     Renderer2D's bind loop now runs through the seam, so the quad shader takes",
    "     the fallback in the same loop that stages offsets for the text one.",
    "  4. A HARNESS FIXTURE has no seam to stage through — everything under tests/ is",
    "     driven outside the render graph inside ScopedSlotBasedShaders, whose comment",
    "     records why the heap's frame-scoped lifetimes have no frame here. A PREFIX",
    "     rule, not entries, so a new fixture inherits the reason.",
    "",
    "A DEFAULT SamplerDesc HAD TO BECOME AN INHERIT, AND GETTING THERE TOOK TWO GOES.",
    "A bindless descriptor BAKES the sampler; the slot path samples with the texture",
    "OBJECT's parameters. So SamplerDesc{} decides what a converted shader sees while",
    "an unconverted reader of the same texture keeps seeing the object's state.",
    "",
    "FIRST ATTEMPT, AND IT WAS WRONG IN AN INSTRUCTIVE WAY. The struct defaulted to",
    "ClampToEdge while OpenGLTexture2D and every framebuffer attachment are GL_REPEAT,",
    "so converting Water.glsl — whose FFT displacement field is TILED — collapsed the",
    "wave field into a handful of enormous flat terraces. Changing the default to",
    "Repeat fixed that and BROKE THE TERRAIN ARRAYS, because OpenGLTexture2DArray is",
    "CLAMP_TO_EDGE for colour and CLAMP_TO_BORDER for depth. There is no majority",
    "answer to default to:",
    "",
    "  Texture2D, framebuffer attachments  REPEAT",
    "  Texture2DArray (colour)             CLAMP_TO_EDGE",
    "  Texture2DArray (depth)              CLAMP_TO_BORDER, opaque-white",
    "  TextureCubemap                      CLAMP_TO_EDGE",
    "  Texture3D                           caller-supplied",
    "  any INTEGER format                  NEAREST, mandatory",
    "",
    "That last row is not a preference. GL makes an integer texture with a LINEAR",
    "filter INCOMPLETE, and an incomplete texture samples as ZERO — texelFetch",
    "included, on Mesa but not NVIDIA. Texture.h::IsIntegerFormat records what that",
    "cost the first time (every Slug glyph vanished on AMD, logs clean); the heap had",
    "reintroduced it for the RG16UI font band texture, the R16UI GTAO Hilbert LUT and",
    "the R32I entity buffer. A sixth face: LinearMipFilter defaults true, which makes a",
    "SINGLE-LEVEL texture incomplete — the hazard ShadowDepthSampler works around by",
    "hand.",
    "",
    "SO THE ANSWER IS NOT A BETTER TABLE, IT IS NOT HAVING ONE. No stated intent ->",
    "glGetTextureHandleARB, which bakes the object's own state and is parity BY",
    "CONSTRUCTION. Stated intent -> glGetTextureSamplerHandleARB, which models a split",
    "heap and is what the two sites that genuinely differ from their texture use.",
    "HeapBinding::CubeSampler(), invented during the first attempt, was deleted rather",
    "than kept — dead vocabulary is worse than none.",
    "",
    "\"NO STATED INTENT\" NEEDED A DISCRIMINATOR, AND THE DISCRIMINATOR NEEDED A",
    "BACKSTOP. Inferring inherit from EQUALITY WITH THE DEFAULTS leaves one thing",
    "inexpressible: a caller wanting Linear+Repeat EXPLICITLY on a colour",
    "Texture2DArray — whose object is ClampToEdge — silently gets the object.",
    "SamplerDesc::Source says which it is, and being part of the defaulted operator==",
    "it flows into the view memo key and the sampler-slot dedup for free.",
    "",
    "Making Source the SOLE test recreates the same class of bug one level over: set",
    "MinFilter, forget Source = Explicit, and the whole sampler is replaced by the",
    "texture's state. Not hypothetical — deleting the line from",
    "HeapBinding::ShadowDepthSampler and running the suite gave 136 PASSING TESTS with",
    "the shadow comparison sampler quietly inheriting. So a desc whose FIELDS say",
    "something is treated as explicit whatever its Source says, and the disagreement is",
    "warned about; the fallback is the pre-discriminator behaviour, so a forgotten line",
    "costs a log line rather than a frame. Pinned by",
    "HeapGpuFixture.SamplerSourceDistinguishesInheritFromExplicitAndSurvivesAForgottenDiscriminator,",
    "verified to fail when the backstop is removed.",
    "",
    "AND THE HEAP NOW REFUSES TO INITIALISE WHEN THE RESERVED NULL BLOCK DOES NOT FIT.",
    "Skipping the null WRITES for a persistent region smaller than",
    "kFirstAllocatableHeapSlot was never enough on its own: NullOffsetForSamplerKind",
    "answers 0..9 unconditionally, so a heap left ENABLED with the block unwritten",
    "hands every unset input an offset addressing either an ordinary persistent view",
    "or, past m_PersistentCapacity, the transient ring — the \"null\" a shader samples",
    "would be whatever view landed there that frame. Refusing is the only honest answer",
    "to a heap too small to hold its own invariants.",
    "",
    "THE DEBT IS COUNTED, NOT HIDDEN. Vulkan has no inherit — a VkSampler must be",
    "described — so every default-desc site is Phase 4 work: 199 of 209 seam call",
    "sites, with Stats::DefaultSamplerInherits counting them at runtime.",
    "",
    "THE FIX HAD A BUG AND ITS OWN TEST CAUGHT IT ON THE FIRST RUN. The guard was",
    "written `(sampler == SamplerDesc{}) && !view.DepthCompare`, but",
    "ViewDesc::DepthCompare DEFAULTS TO TRUE — true means \"whatever the resource's own",
    "view would give\" and false is the deliberate raw-depth override. So the guard",
    "inverted itself and disabled inheriting for every ordinary view. It now compares",
    "against a default-constructed ViewDesc instead of against a literal, which also",
    "survives the default flipping.",
    "",
    "THE MEASUREMENT IS THE TRANSFERABLE PART. This file already retired the LIVE",
    "ON-vs-OFF A/B, because on an animated scene the same-config noise floor is as",
    "large as the signal. The suite's FIXED-CAMERA visual-evidence PNGs have a",
    "same-config floor of exactly 0.000 — bit-identical across consecutive runs — so",
    "the cross-config number means something on its own:",
    "",
    "  noise floor (heap ON, two consecutive runs)   RMSE  0.000   0.000% of pixels",
    "  heap OFF vs ON, before                        RMSE 22.670  39.93%",
    "  heap OFF vs ON, after                         RMSE  2.783   1.76%",
    "",
    "And the residual was characterised rather than waved off: amplified 6x it is",
    "HAIRLINE CONTOURS around the foam boundaries, max 58 with nothing above 64 — a",
    "smoothstep threshold crossing differently because the two compile routes round",
    "the last bit differently. That is the §7a-bis phenomenon applied to a threshold",
    "instead of to depth, and it is a property of having two compilers, not of the",
    "binding model.",
    "",
    "FOUR THINGS GENERALISE: when a neutral struct's default has to agree with a",
    "backend, ask the backend rather than guess well (six rows, no majority); a fix",
    "that turns one bug into another is telling you about the SHAPE, not the value;",
    "a green suite is not evidence about pixels, since every image above came from a",
    "passing test in both configurations before AND after; and a ZERO noise floor is",
    "what makes a small number readable — 2.783 would be invisible against the live",
    "scene's floor of 9.74.",
    "",
    "TWO SHADERS ARE CONVERTED BUT NOT EXERCISED BY THE SUITE, and they are not the",
    "same case. Terrain_Erosion.comp is created only by the editor's terrain brush,",
    "so the honest claim is 'editor-verified': the live run under OLO_RHI_BINDLESS=1",
    "logs it on the route, among 86 graphics + 14 compute shaders, with 0 shader",
    "errors, 0 .bindless.failed.glsl dumps and 0 'Not a valid texture'.",
    "",
    "Basic3D.glsl is a THIRD category and the claim for it is weaker still: its only",
    "consumer is MaterialAsset's fallback when the DefaultPBR shader is missing, so",
    "nothing instantiates it in a healthy build — not the suite, not the editor. It",
    "is converted and it compiles; whether its bindless variant renders is untested",
    "and will stay that way until something breaks badly enough to reach for it.",
    "Recorded rather than glossed, because 'converted' and 'verified' are different",
    "claims and the gap between them is exactly what a silent fallback hides."
  ],
  "measured_on_commit": "11d7cea7",
  "measured_on_date": "2026-07-30",
  "step3_counters_measured_on_commit": "26d2fec0",
  "step3_counters_measured_on_date": "2026-08-02",
  "sweep_gl_calls": 0,
  "sweep_glad_includes": 0,
  "tools_gl_calls": 236,
  "debug_escape_hatch": 0,
  "sweep_renderer_id": 168,
  "facade_native_id_params": 0,
  "backend_resolve_hatch": 0,
  "phase3_counters_measured_on_date": "2026-08-04",
  "sweep_bind_texture_sites": 22,
  "phase3_bucket1_closed_on_date": "2026-08-07"
}
