name: Sanitizers
on:
  push:
    branches: [master]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  checks: write

jobs:
  asan-windows:
    name: ASan (Windows/MSVC)
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v6

    - name: Install Python 3.10
      id: py
      uses: actions/setup-python@v6
      with:
        python-version: '3.10'

    - name: Install python dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install jinja2

    - name: Setup Vulkan SDK
      uses: ./.github/actions/setup-vulkan

    - name: Configure CMake (ASan enabled)
      run: >
        cmake -S . -B build
        -DPython_EXECUTABLE="${{ steps.py.outputs.python-path }}"
        -DOLO_ENABLE_ASAN=ON
        -DBUILD_TESTS=ON

    - name: Build (Release)
      run: cmake --build build --config Release --target OloEngine-Tests --parallel

    - name: Create test results directory
      run: New-Item -ItemType Directory -Force -Path "${{github.workspace}}/test_results"
      shell: pwsh

    - name: Add MSVC ASan runtime to PATH
      shell: pwsh
      run: |
        $vsPath = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -property installationPath
        $msvcDir = Get-ChildItem "$vsPath\VC\Tools\MSVC" -Directory | Sort-Object Name -Descending | Select-Object -First 1
        $binDir = "$($msvcDir.FullName)\bin\Hostx64\x64"
        Write-Host "Adding MSVC bin to PATH: $binDir"
        echo "$binDir" >> $env:GITHUB_PATH

    - name: Run tests under ASan
      working-directory: ${{github.workspace}}/build/OloEngine/tests
      run: ctest -C Release --output-on-failure --rerun-failed --exclude-regex "NetworkIntegration|TaskSystemTest|TaskEventTest|NestedTasksTest"
      env:
        ASAN_OPTIONS: halt_on_error=1
        GTEST_OUTPUT: xml:${{github.workspace}}/test_results/

    - name: Test report
      if: always()
      uses: dorny/test-reporter@v3
      with:
        name: ASan (Windows)
        path: test_results/*.xml
        reporter: java-junit
        fail-on-error: false
        list-tests: 'failed'

  asan-lsan-linux:
    name: ASan + LSan (Linux/Clang)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Install Clang 19
      run: |
        wget --max-redirect=0 -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
        echo "deb https://apt.llvm.org/noble/ llvm-toolchain-noble-19 main" | sudo tee /etc/apt/sources.list.d/llvm-19.list

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          clang-19 lld-19 \
          libvulkan-dev vulkan-validationlayers \
          libgl-dev libglu1-mesa-dev \
          libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev \
          libwayland-dev wayland-protocols libxkbcommon-dev \
          glslang-dev libshaderc-dev libspirv-cross-c-shared-dev spirv-tools \
          python3 python3-pip
        python3 -m pip install --upgrade pip jinja2

    - name: Configure CMake (ASan + LSan)
      run: >
        cmake -S . -B build
        -DCMAKE_CXX_COMPILER=clang++-19
        -DCMAKE_C_COMPILER=clang-19
        -DCMAKE_BUILD_TYPE=Debug
        -DOLO_ENABLE_ASAN=ON
        -DBUILD_TESTS=ON

    - name: Build tests
      run: cmake --build build --target OloEngine-Tests --parallel 2

    - name: Create test results directory
      run: mkdir -p "${GITHUB_WORKSPACE}/test_results"

    - name: Run tests under ASan + LSan
      working-directory: ${{github.workspace}}/build/OloEngine/tests
      run: ctest --output-on-failure --rerun-failed --exclude-regex "NetworkIntegration"
      env:
        ASAN_OPTIONS: halt_on_error=1:detect_leaks=1
        LSAN_OPTIONS: suppressions=${{github.workspace}}/.github/sanitizer-suppressions/lsan.txt
        GTEST_OUTPUT: xml:${{github.workspace}}/test_results/

    - name: Test report
      if: always()
      uses: dorny/test-reporter@v3
      with:
        name: ASan + LSan (Linux)
        path: test_results/*.xml
        reporter: java-junit
        fail-on-error: false
        list-tests: 'failed'

  ubsan-linux:
    name: UBSan (Linux/Clang)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Install Clang 19
      run: |
        wget --max-redirect=0 -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
        echo "deb https://apt.llvm.org/noble/ llvm-toolchain-noble-19 main" | sudo tee /etc/apt/sources.list.d/llvm-19.list

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          clang-19 lld-19 \
          libvulkan-dev vulkan-validationlayers \
          libgl-dev libglu1-mesa-dev \
          libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev \
          libwayland-dev wayland-protocols libxkbcommon-dev \
          glslang-dev libshaderc-dev libspirv-cross-c-shared-dev spirv-tools \
          python3 python3-pip
        python3 -m pip install --upgrade pip jinja2

    - name: Configure CMake (UBSan)
      run: >
        cmake -S . -B build
        -DCMAKE_CXX_COMPILER=clang++-19
        -DCMAKE_C_COMPILER=clang-19
        -DCMAKE_BUILD_TYPE=Debug
        -DOLO_ENABLE_UBSAN=ON
        -DBUILD_TESTS=ON

    - name: Build tests
      run: cmake --build build --target OloEngine-Tests --parallel 2

    - name: Create test results directory
      run: mkdir -p "${GITHUB_WORKSPACE}/test_results"

    - name: Run tests under UBSan
      working-directory: ${{github.workspace}}/build/OloEngine/tests
      run: ctest --output-on-failure --rerun-failed --exclude-regex "NetworkIntegration|NetworkManager|NetworkThreadDispatch|TaskConcurrencyLimiter|LowLevelTaskUserData"
      env:
        UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
        GTEST_OUTPUT: xml:${{github.workspace}}/test_results/

    - name: Test report
      if: always()
      uses: dorny/test-reporter@v3
      with:
        name: UBSan (Linux)
        path: test_results/*.xml
        reporter: java-junit
        fail-on-error: false
        list-tests: 'failed'

  tsan-linux:
    name: TSan (Linux/Clang)
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
    - uses: actions/checkout@v6

    - name: Install Clang 19
      run: |
        wget --max-redirect=0 -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
        echo "deb https://apt.llvm.org/noble/ llvm-toolchain-noble-19 main" | sudo tee /etc/apt/sources.list.d/llvm-19.list

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          clang-19 lld-19 \
          libvulkan-dev vulkan-validationlayers \
          libgl-dev libglu1-mesa-dev \
          libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev \
          libwayland-dev wayland-protocols libxkbcommon-dev \
          glslang-dev libshaderc-dev libspirv-cross-c-shared-dev spirv-tools \
          python3 python3-pip
        python3 -m pip install --upgrade pip jinja2

    - name: Configure CMake (TSan)
      run: >
        cmake -S . -B build
        -DCMAKE_CXX_COMPILER=clang++-19
        -DCMAKE_C_COMPILER=clang-19
        -DCMAKE_BUILD_TYPE=Debug
        -DOLO_ENABLE_TSAN=ON
        -DBUILD_TESTS=ON

    - name: Build tests
      run: cmake --build build --target OloEngine-Tests --parallel 2

    - name: Create test results directory
      run: mkdir -p "${GITHUB_WORKSPACE}/test_results"

    - name: Run tests under TSan
      working-directory: ${{github.workspace}}/build/OloEngine/tests
      run: ctest --output-on-failure --rerun-failed --exclude-regex "NetworkIntegration|WorkerRestart"
      env:
        TSAN_OPTIONS: halt_on_error=1:second_deadlock_stack=1
        GTEST_OUTPUT: xml:${{github.workspace}}/test_results/

    - name: Test report
      if: always()
      uses: dorny/test-reporter@v3
      with:
        name: TSan (Linux)
        path: test_results/*.xml
        reporter: java-junit
        fail-on-error: false
        list-tests: 'failed'
